# Copy to .env and fill in. .env is gitignored; never commit real values.
# Add every new variable here (with a comment) in the same PR that introduces it.

# ALPACA_API_KEY=
# ALPACA_API_SECRET=
# ALPACA_PAPER=true
# ANTHROPIC_API_KEY=
# SEC_EDGAR_USER_AGENT="Your Name your@email"
