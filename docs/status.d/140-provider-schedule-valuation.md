- Phase 3 T37: `DataProvider` protocol (every method takes a tz-aware `t`) and `GapReading` in `backtest/provider.py`; `schedule.rebalance_sessions`/`fill_session`/`read_time` from the XNYS calendar; `valuation.value_positions` (dollar positions priced within one marking frame, last mark kept), `carry_to_fill` and `stitched_returns`; in-memory `FakeProvider` recording every call (#140)
