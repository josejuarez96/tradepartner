- #181 Ingest writes corporate actions by identity: `parse_corporate_actions` sets `source_action_id` from Alpaca's `id`; `ingest._add_actions` (daily ingest and backfill) matches revisions by id across ex-dates, re-asks the source over a window widened to a re-dated event's stored ex-dates so a replay never reverts it, writes a source cancel, and turns an id-less re-date (one key gone, one new, same security and type) or an id-less key gaining an id into a cancel plus a replacement at `ingested_at`; an absent key is never cancelled. A month-by-month replay of the fixture universe ends with its recorded live events
