- #181 Ingest writes corporate actions by identity: `parse_corporate_actions` sets `source_action_id` from Alpaca's `id`; `ingest._add_actions` (daily ingest and backfill) matches revisions by id across ex-dates; `_replay_plan`/`_replay_actions` re-ask the source over a window widened to a re-dated event's stored ex-dates (backfill with no store connection open) so a replay never reverts it, and the write refuses a store that changed since; a source cancel is written; an id-less re-date, or an id-less key gaining an id (same or new ex-date), is a cancel plus a replacement at `ingested_at`, each old key cancelled once; an absent key is never cancelled, and an id-less re-date out of the window is not seen (documented). A month-by-month replay of the fixture universe ends with its recorded live events
