- Phase 2 T11b: `EdgarFilingSource` index and snapshot (`adapters/edgar_source.py`): quarterly `form.idx` from `edgar.index_first_year`, cached gzipped once settled (`edgar.index_settle_days`), open-quarter 404 empty; keeps issuer CIKs with all their forms (`.skipped_filers` counts the rest), exchange copies of 25-NSE dropped; acceptance times from submissions cached per CIK under `PARSER_VERSION`, bulk `submissions.zip` above `edgar.bulk_stamp_threshold_ciks` then a per-CIK top-up; unstamped rows on `.unstamped_filings`, cached as unstampable only after their quarter settled; `edgar_raw` gains `cached_filing_path`, `write_atomic`, `bulk_submissions`, `bulk_company_facts` and a present-file return in `download_filing_file`; T11c methods raise `NotImplementedError` (#184)
