- Phase 3 T40: `tests/lookahead/test_backtest_invariance.py`: `engine.run` over `StoreProvider` on the fixture universe at all 41 rebalance ends equals the run on `TruncatedStore.at(close(T_i))` (registry on the untruncated store) and the prefix of the full-range run (equity, weights, rebalances, position values, marking frames, targets, both cost levels); a bar revision and a dividend revision known an hour after close(2019-01-31) each leave the run to 2019-01-31 unchanged and change the run to 2019-02-28 (#191)
