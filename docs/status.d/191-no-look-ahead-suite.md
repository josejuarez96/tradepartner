- Phase 3 T40: `tests/lookahead/test_backtest_invariance.py`: `engine.run` over `StoreProvider` at all 41 fixture rebalance ends equals the run on `TruncatedStore.at(close(T_i))` (registry on the untruncated store) and the prefix of the full-range run (equity, weights, rebalances, position values, marking frames, targets, both cost levels), on a store seeded with bar revisions, restated and late dividends on every held name at 16 rebalances; a bar revision at T_i, a dividend revision ex T_i and one ex inside step i, each known an hour after close(2019-01-31), leave the run to 2019-01-31 unchanged and change the run to 2019-02-28. A plan read one rebalance late is not caught, see #201 (#191)
