- #216 Plan amendment: T11c takes cover pages, per-class shares and SIC from the SEC Financial Statement and Notes data sets per #174, with per-filing downloads only for the lag window and SGML headers only for registration forms; classification reads `edgar.header_forms`; new T11d holds delistings, the failure policy for a filing that fails the same way every run (skip, count, quarantine after `edgar.max_filing_failures`, fail the chunk above `edgar.max_failed_filing_share`) and the owner's backfill measurement; T19 depends on T11d; owner decisions (1) and (2) recorded by the merge
