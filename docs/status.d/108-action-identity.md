- #108 Corporate action identity: `corporate_actions` gains `source_action_id` and `cancelled` (schema v4; a version-2 or version-3 store is migrated in one transaction by rebuilding the table, every row kept as an id-less live row), so a re-dated action (same source id, new ex-date) is a revision that applies once, and a cancelled revision withdraws an event. An id-less re-date is a cancel plus a replacement row stamped at its ingest, never at a proxy. The store holds one row per identity per `known_at`, and as-of reads pick the latest revision per identity before filtering on ex-date (PR #111)
