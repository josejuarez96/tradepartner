- Phase 2 T12: Alpaca parsers and `AlpacaPriceSource` (`adapters/alpaca_prices.py`): raw bars known at the session close with `source` per feed (`alpaca_sip`/`alpaca_iex`), splits and cash dividends on the first-seen proxy, symbols resolved through master listings by (ticker, session); zero-volume placeholder bars, unresolved symbols and unstored action categories reported, not stored (#137, PR #TBD)
