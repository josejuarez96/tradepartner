- Phase 2 T21: data-health page (`dashboard/health_page.py`, the shell's "Data health" entry) per the dashboard standard: "as of" / "last updated" header with a stale chip, KPI row (coverage, interior gaps, delisted names, last update), missing share per session against `ingest.max_missing_share` over a chosen window, bars per session, integrity rules as status chips, per-source ingest status, survivorship gap and side categories, unclassifiable and `snapshot_static` reliance, liquidity rule and fill price, gap/delisted/missing tables; tokens in `dashboard/theme.py` mirrored by `.streamlit/config.toml` (light and dark), charts in Altair (#205, PR #207)
