- Phase 3 T36: `backtest/holdout.py`, pure window, holdout and gap-gate decisions: `refused_window` outside `[in_sample_start, holdout.end]`, `refused_holdout` without `--spend-holdout` and a reason or on a same-hypothesis repeat without `--holdout-repeat`, `holdout_repeat` on any prior family spend, `refused_gap` above the frozen count-share threshold at any month-end session unless overridden with a reason, `needs_gap` before the gap is read; default in-sample window ends at the last rebalance session before `holdout.start` (#145)
