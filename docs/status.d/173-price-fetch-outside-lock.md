- #173 The daily Alpaca chunk fetches before its write transaction: `ingest._fetch_prices` (a `prepare`) reads names and the action replay plan on short read-only connections and fetches bars and actions with no connection open, then `_write_prices` only writes; a reader and a writer from another process open the store while the price side fetches. `_read` moved from `backfill` to `ingest`
