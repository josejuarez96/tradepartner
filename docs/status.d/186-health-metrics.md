- Phase 2 T18: `health_report(conn, t, settings)` (`health.py`) shared by the CLI and page: last ok and latest ingest run per source, coverage of live common and benchmark names at the last completed session (ingest's staleness population), interior bar gaps, `survivorship_gap` with its side categories, unclassifiable and unclassified names, `snapshot_static` reliance per table, delisted names with form and dates, liquidity-rule and fill-price settings; nine named integrity rules (non-null `known_at` and `source`, `known_at <= ingested_at`, provenance in the table set, bars on sessions, no duplicate bars, non-overlapping listings (same start, or a Form 25 filed more than `master.transfer_window_sessions` after the next listing started), no bars after a delisting's `effective_on`, guarded SIC = charter), each failing on an injected violation; as-of metrics truncation-invariant (#186)
