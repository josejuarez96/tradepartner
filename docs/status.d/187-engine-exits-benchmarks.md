- Phase 3 T37c: engine exits and benchmarks (`backtest/engine.py`): a held name whose current listing at the read (latest `valid_from`, so a ticker change does not hide a delisting) is not `listed` is sold at its last close on its final session, one with no bar on the last `backtest.stale_exit_sessions` sessions through T_{i+1} at its last bar, each paying costs and booked at F_i when the exit session is earlier; a known transfer is held through; SPY and MTUM bought once at F_0 after costs and carried through every marking frame (total return, cash-free rows); exit counts, cost and turnover in the rebalance rows (#187, PR #190)
