- Phase 2 T16: `ingest_session` brings the store to the expected session (last completed minus `ingest.settle_delay_minutes`), EDGAR then Alpaca, one atomic `open_for_write` chunk per source, halting at the first stale/failed/locked source with only its `ingestion_runs` row; stale on a missing reference-symbol bar or over `max_missing_share` of listed common and benchmark names missing; re-runs add nothing (rows written only when an as-of read changes; revisions at `ingested_at`, A→B→A kept); shares facts go to common classes only, by class letter; run messages redacted and capped. Real EDGAR `FilingSource` still missing, see #163 (#157, PR #164)
