- Phase 2 T16: `ingest_session` brings the store to the expected session (last completed minus `ingest.settle_delay_minutes`), EDGAR then Alpaca, one atomic `open_for_write` chunk per source, halting at the first stale/failed/locked source with only its `ingestion_runs` row; stale on a missing reference-symbol bar or over `max_missing_share` listed names missing; re-runs add nothing (rows written only when an as-of read changes; bar/action revisions at `ingested_at`); shares facts mapped to classes by class letter. Real EDGAR `FilingSource` still missing, see #163 (#157)
