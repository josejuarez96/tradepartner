- Phase 2 T15: `survivorship_gap(conn, t, settings)` over W = (previous rebalance, T]: L = common non-benchmark names on `universe.exchanges` listed at any session in W, M = live names with no bar at T plus Form 25 delistings in W whose last bar is more than `gap.missing_tail_sessions` before the last session before filing; count and size share, side categories (unclassifiable, truncated history, stale shares) disjoint from M; truncation-invariant at every fixture `known_at` (#143, PR #152)
