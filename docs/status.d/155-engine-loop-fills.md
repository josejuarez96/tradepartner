- Phase 3 T37b: `backtest/engine.py` `run(params, provider, start, end, handle, cost_levels)` reads the provider once per step at rebalance closes and evaluates every cost level from that read set (carry, fill, value); `backtest/fills.py` `apply_trades` (sells first, buys sized after costs, missing fills); rebalance rows with late and dropped dividends; `DataProvider.raw_prices` for share counts; exits and benchmarks left to T37c (#155)
