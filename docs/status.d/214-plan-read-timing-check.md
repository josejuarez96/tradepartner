- Phase 3 T40b: plan-read timing check (`tests/lookahead/test_backtest_plan_timing.py`): every rebalance's plan (targets for its fill session, the six plan fields) is unchanged on a benchmark-exempt store cut at its own read time, through both `_plan` call sites (two- and three-rebalance windows, T_0 included); teeth for a late plan read (T40's truncation still passes) and a late signal read with a control; engine mutations at either call site fail it; 16 s on the fixture (#214)
