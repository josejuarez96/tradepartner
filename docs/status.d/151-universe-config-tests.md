- Phase 2 T14: every `universe.*` key overridden one at a time, through `Settings` and through `UNIVERSE__*` env vars, pins the exact names that enter or leave at a fixture T and the rule each now fails; a coverage test fails when a key has no case; guarded `exclude_sic_ranges` refuses every override; `tests/test_no_literals.py` holds the AST literal check for `universe.py` and `gap.py` (skipped until T15 adds it) (#151)
