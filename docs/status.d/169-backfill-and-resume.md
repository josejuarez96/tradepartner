- Phase 2 T17: `backfill(since=...)`: EDGAR as one full-history chunk (never limited by `since`), Alpaca as one chunk per calendar month fetched with no store connection open and committed under a short write lock; each month writes an `ingestion_runs` row (mode `backfill`, cursor `since=…;through=…`); a failed or stale month commits nothing, earlier months stand, and a re-run with the same `since` resumes the day after the last `ok` month; reads retry a writer's lock for `store.lock_retry_seconds` (#169)
