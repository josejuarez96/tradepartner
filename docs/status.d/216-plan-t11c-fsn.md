- #216 Plan amendment: the cover-page scope of T11c is rebuilt on the SEC Financial Statement and Notes data sets per #174, split into T11c (fetch, parse into unstamped per-CIK caches, recorder target), T11d (cover pages and SIC headers served from those caches stamped at the submissions acceptance, per-filing downloads only for what FSN does not hold, classification on `edgar.header_forms`), T11e (facts) and T11f (delistings, the policy for a filing that fails the same way every run, owner-measured backfill cost); T19 depends on T11f; spec master table notes the new sources; owner decisions (1) and (2) recorded by the merge; two spec-critic passes
