# Plan: Backtest engine and trial registry (Phase 3)

**Spec:** [specs/backtest.md](../specs/backtest.md)  ·  **Status:** Draft

## Approach (short)

Build the pure parts first, then the store-facing parts, then the checks, then the surfaces. Config and dependencies; the registry schema and its append-only API; metrics with deflated Sharpe, the cost model, the signal and portfolio rules, the holdout decision, the valuation and schedule helpers: all pure functions over synthetic inputs, none of which needs Phase 2 to finish. The engine loop is written against a `DataProvider` protocol with a fake provider in tests, so it too starts early. Only the store-backed provider needs `universe_as_of` (T13), `survivorship_gap` (T15) and read-time delistings (T8b); everything after it (run orchestration, invariance suite, oracle, CLI) waits on that one task; the results writer (T39b) does not, so it lands before orchestration and orchestration depends on it. The two pages read registry tables and start as soon as the registry API merges. Two owner tasks bracket the phase: the price-vendor ADR (T29) and the real-store run (T45b); agents never enter the main checkout (CLAUDE.md rule 9), so no agent task touches the owner's store.

**What may start while Phase 2 runs.** Any task whose dependencies are all ticked on `origin/main` is claimable the moment this plan merges (teams.md): T29 (owner), T30 (depends on T1) and T31 (on T4) immediately; T31b, T32, T33, T34, T37 next; T35, T36, T43 once T31b and T30 merge; T37b once T31b, T33, T34 and T37 merge; T39b once T31b, T32 and T37c merge. T38 (which needs only T37's protocol and T31b's handle, not the engine) is the gate to the Phase 2 chain; T42 also waits on the Phase 2 CLI (T19). The H1 hypothesis file is its own docs task (T35b), where the owner answers spec open questions 1, 2, 8 and 10; only T45b depends on it. Starting early is deliberate: pure functions over synthetic inputs test the same whether or not real data exists, and the engine's correctness does not depend on the vendor decision. Spec open question 9 puts that choice to the owner; merging this plan records it, and `DEPENDS_RE` enforces the gate because T29 sits in T45's and T45b's dependencies.

**Price-vendor ADR (T29, owner).** No build task depends on it and none writes a vendor adapter (out of scope). If the ADR chooses a vendor, its adapter is a new plan task added by amendment before T45b runs.

Design choices: engine as a rebalance loop over a `t`-parameterised provider, positions carried as dollar values priced within one adjusted-as-of-`t` frame per step ([ADR 0003](../decisions/0003-data-adapters-local-first.md) rule 2, [ADR 0006](../decisions/0006-universe-and-cadence.md)); a short-lived read-only connection per step so a run never holds the ingest job's lock; our own engine with `bt` as a test-only oracle over a stitched frame, at a tolerance pinned in the test file ([ADR 0004](../decisions/0004-tooling-adopt-avoid.md); the roadmap's "configured tolerance" means that constant, not a `Settings` key); registry tables in the DuckDB store, append-only, trial handle before data ([ADR 0005](../decisions/0005-objective-benchmark-stop-criteria.md)), verified through a registry connection separate from the data connection so the look-ahead harness's fact-table-only store still works; frozen parameters per hypothesis including the holdout window and gap threshold; deflated Sharpe hand-written on two bases and tested against reference values. **Metrics library:** ADR 0004 leaves the choice between `empyrical-reloaded` and `quantstats` to this plan. This plan picks **`empyrical-reloaded`** (0.5.12, pinned): pure functions over series with no plotting stack; on PyPI (checked 2026-09-25) its runtime requirements are numpy, pandas, scipy, bottleneck and peewee, with `yfinance` only as an optional extra, whereas `quantstats` requires `yfinance`, matplotlib and seaborn outright and ADR 0004 avoids `yfinance` outside spikes. `bt==1.2.3` goes in the dev dependency group only; it pulls `ffn`, which pulls `yfinance` and `scikit-learn`, into the dev environment and nowhere else. An AST test keeps `bt`, `ffn` and `yfinance` out of `src/`, and the autouse no-network fixture keeps them off the network.

**All dependencies are added in T30** (`empyrical-reloaded` runtime, exact pin; `bt` dev, exact pin) so no later task touches `pyproject.toml` or `uv.lock`. `uv.lock` lines never count toward the ~400-line budget. T30 also appends a dated amendment note to ADR 0004 recording the metrics choice and the oracle's fill-session-only comparison under `open` fills.

**Phase 2 interface note (required follow-up).** T38 passes the trial's frozen `Settings` into every as-of call. `adjusted_prices_as_of` already takes `settings`; `universe_as_of` (T13) and `survivorship_gap` (T15) must accept one too. This plan does not edit `data-foundation.md`; issue #102 asks that T13 and T15 land with a `settings` parameter, or a `size:S` follow-up adds it before T38 starts.

Package layout: `src/tradepartner/backtest/{__init__,metrics,costs,signals,portfolio,hypothesis,holdout,provider,schedule,valuation,fills,engine,store_provider,run,results}.py`, `src/tradepartner/store/registry.py`, `src/tradepartner/dashboard/{backtest_page,trials_page}.py`, `docs/hypotheses/`, `docs/templates/hypothesis.md`; tests under `tests/backtest/`, `tests/lookahead/test_backtest_invariance.py`, `tests/oracle/test_bt_oracle.py`, `tests/dashboard/`, with the temp-file fixture store (`fixture_store_path`) added to the root `tests/conftest.py` (T39b) so `tests/oracle/` and `tests/backtest/` both see it, and a fixture hypothesis file under `tests/fixtures/hypotheses/`.

## Tasks

Each task = one branch = one PR (~≤400 lines; `uv.lock` excluded). Tasks with no shared files may run in parallel. Task ids start at T29 because ids are global across plans. "Owner" tasks need the owner's machine, keys or budget decision and are never run by `implementer`.

- [ ] **T29 (owner): Price-vendor ADR.** Files: `docs/decisions/0008-price-vendor.md` (vendor or "no vendor", data budget, delisted-coverage grade from G8, terms on local storage and derived artefacts; "no vendor" also amends charter principle 4 per ADR 0003 rule 8), `docs/charter.md` (only if "no vendor") · Tests: none; a data test of the shortlisted vendor's delisted coverage (G8 follow-up 2) is attached if a vendor is chosen · Depends on: n/a · Review: spec-critic. Not size-limited: it is the owner's budget decision, due at Phase 3 start (ADR 0003 rule 8).
- [x] **T30: Phase 3 config and dependencies.** Files: `src/tradepartner/config.py` (`HypothesesConfig` with the enumerated `families`, `StrategyConfig`, `CostsConfig`, `HoldoutConfig` with `start`/`end` defaulting to null, `BacktestConfig`, `MetricsConfig` without `periods_per_year`; sensitivity list non-negative), `pyproject.toml`, `uv.lock` (`empyrical-reloaded==0.5.12` runtime; `bt==1.2.3` in the dev group; mypy `ignore_missing_imports` for `empyrical`), `docs/decisions/0004-tooling-adopt-avoid.md` (append "Amendment 2026-xx-xx": metrics library chosen and why, `bt` dev-only with its transitive `yfinance`, oracle compares fill sessions only under `open` fills) · Tests: `tests/test_config.py` (every new key has its documented default; `holdout.*` null by default; a holdout end before its start, a negative sensitivity and a family outside the list rejected; no `.env` still loads; `.env.example` names no `HOLDOUT__` key), `tests/test_no_forbidden_imports.py` (AST: nothing under `src/` imports `bt`, `ffn` or `yfinance`); PR attaches `uv tree` for the runtime closure · Depends on: T1 · Review: quant-auditor.
- [x] **T31: Registry schema and migration.** Files: `src/tradepartner/store/schema.py` (eight registry tables in `REGISTRY_TABLE_NAMES`, disjoint from `TABLE_NAMES`; `CURRENT_SCHEMA_VERSION = 3`, because #83 took version 2 for `corporate_actions.announced_at`; additive migration: a version-2 store gets the new tables and an appended version row, nothing else changes; a version-1 store raises `SchemaVersionError` (rebuild, #83); a read-only connection reports `RegistryNotInitialised` instead of migrating; docstring: a later fact-table DDL change goes to version 4) · Tests: `tests/store/test_registry_schema.py` (tables and version after a fresh init and after opening a version-2 store; fact tables byte-identical after migration, fact DDL pinned by hash; read-only open of a version-2 store raises the named error; `REGISTRY_TABLE_NAMES ∩ TABLE_NAMES = ∅`; `tests/lookahead/test_asof_invariance.py` still passes unchanged) · Depends on: T4 · Review: quant-auditor.
- [x] **T31b: Registry API and trial handle.** Files: `src/tradepartner/store/registry.py` (`TrialHandle` constructed only by `open_trial`; `register_hypothesis` (refuses `family=oracle` when the connection's database file is `settings.store.path`), `open_trial` (refuses `synthetic=True` likewise), `close_trial`, `write_result`, `write_metrics`, `write_equity`, `write_weights`, `write_rebalances`, `record_decision`, `family_sharpes` returning both bases from `sharpe_monthly` and `sharpe_monthly_excess_spy` over the latest `ok` trial per (parameter hash, window) pair, `family_holdout_spends`, `list_trials` with `unfinished` derived; inserts and reads only; `code_version` from `git rev-parse HEAD` with a dirty flag, `unknown` outside a checkout; `store_max_ingested_at` captured at open), `docs/research/trial-registry.md` (now a pointer to the tables and the `trials` command) · Tests: `tests/store/test_registry.py` (no `UPDATE`/`DELETE` in the module text; ids increase; closing twice raises; a handle cannot be built outside `open_trial`; oracle family and synthetic refused on `settings.store.path` and accepted on a temp file; `family_sharpes` N over all `ok` non-synthetic `in_sample` rows, V per basis over latest-per-pair, an annualized value never read; `family_holdout_spends`; `unfinished` listing; decision rows; code version and dirty flag) · Depends on: T31 · Review: quant-auditor. Near the budget: if over, split the `write_*` functions and their tests into a `T31c` by amendment.
- [x] **T32: Metrics and deflated Sharpe.** Files: `src/tradepartner/backtest/{__init__,metrics}.py` (`MONTHS_PER_YEAR = 12`; every req 7 key via `empyrical` where it exists; `expected_max_sharpe`, `probabilistic_sharpe`, `deflated_sharpe(basis)` on `raw` and `excess_spy` with the `psr` basis when fewer than two pairs; `red_flag`; γ = `numpy.euler_gamma`) · Tests: `tests/backtest/test_metrics.py` (hand-computed values for every key on a synthetic series; `sharpe_annual = sharpe_monthly × √12`; the spec's DSR reference values to 1e-6; N=1 reduces to PSR; monotonicity in N, kurtosis and negative skew; red flag either side of the threshold; AST literal check on `metrics.py`) · Depends on: T30 · Review: quant-auditor.
- [x] **T33: Cost model.** Files: `src/tradepartner/backtest/costs.py` (`trade_cost(notional, shares, per_side_bps, commissions)`, `buy_notional_after_costs(cash, per_side_bps, commissions)` so cash never goes negative, `sensitivity_levels(settings)`) · Tests: `tests/backtest/test_costs.py` (three-trade fixture against hand-computed values per component, ADR 0004; zero cost is the identity; post-cost cash ≥ 0 at the ladder maximum; levels sorted and include the base; AST literal check on `costs.py`) · Depends on: T30 · Review: quant-auditor.
- [x] **T34: Momentum signal and portfolio construction.** Files: `src/tradepartner/backtest/signals.py` (`momentum_12_1(frame, t_session, formation_months, skip_months)` over an adjusted price frame and the calendar's month-end sessions; excluded names reported), `src/tradepartner/backtest/portfolio.py` (`target_weights(scores, top_fraction, weighting)`: long-only, ≥ 0, sum ≤ 1, ties by `security_id` ascending; `drifted_weights`, `trades_from(target, drifted)`) · Tests: `tests/backtest/test_signals.py` (skip month excluded; missing formation bar excluded and counted; a session after `t_session` in the frame is never read), `tests/backtest/test_portfolio.py` (weights properties; tie order; empty score set gives all cash) · Depends on: T30 · Review: quant-auditor.
- [x] **T35: Hypothesis template and pre-registration.** Files: `docs/templates/hypothesis.md`, `docs/README.md` (one row for `hypotheses/`), `tests/fixtures/hypotheses/fixture-momentum.md` (a complete fixture hypothesis file naming every required key, `family=momentum`), `src/tradepartner/backtest/hypothesis.py` (parse the file's parameter block; refuse a file missing any required key; frozen set = file values for named keys, live `Settings` for the rest of the spec's frozen list; SHA-256 of file and parameters; `register` through `store.registry`; `load_frozen(slug) -> Settings`) · Tests: `tests/backtest/test_hypothesis.py` (fixture file parses; frozen set is exactly the spec's key list incl. `metrics.*`, `master.*`, `benchmarks`, `alpaca.historical_feed`; a file without `holdout.end` or a `costs.*` key is refused; file values win over `Settings`; changed file gives a new hypothesis; frozen values survive environment overrides of `HOLDOUT__START`, `GAP__COUNT_SHARE_THRESHOLD`, `STRATEGY__TOP_FRACTION`, `ADJUST__MAX_PRIOR_CLOSE_GAP_SESSIONS`, `BENCHMARKS` and `ALPACA__HISTORICAL_FEED`; family outside the list refused; unregistered slug refused) · Depends on: T30, T31b · Review: quant-auditor.
- [ ] **T35b: H1 hypothesis file.** Files: `docs/hypotheses/h1-momentum-12-1.md` (rationale from handoff H1 and G1's primary sources; `in_sample_start`, `holdout.start`, `holdout.end` and every `strategy.*` and `costs.*` key named explicitly, with the owner's answers to spec open questions 1, 2, 8 and 10 recorded in the PR; expected magnitudes and red flags; the ~84-in-sample / ~33-holdout-month power arithmetic; the prior-evidence disclosure of holdout-period results already seen; retirement condition) · Tests: `tests/backtest/test_h1_file.py` (the file parses through `hypothesis.py` with every required key present and `family=momentum`; no registration) · Depends on: T35 · Review: spec-critic; the owner accepts by merging. Docs only; Fable tier per agents.md.
- [x] **T36: Holdout, window and gap-gate decisions.** Files: `src/tradepartner/backtest/holdout.py` (`window_touches_holdout`, `default_in_sample_window`, `decide(window, frozen, flags, reasons, gap_series, prior_spends) -> Decision` with `run`, `refused_window`, `refused_holdout`, `refused_gap`, the `holdout_repeat` marker and the same-hypothesis repeat rule; pure) · Tests: `tests/backtest/test_holdout.py` (start before `in_sample_start` refused; end after `holdout.end` refused; overlap at either edge refused; default window ends at the last rebalance session before the frozen `holdout.start`; flag without reason refused; gap above the frozen threshold at one date refused unless overridden; a prior family spend marks repeat; a prior spend by the same hypothesis needs `--holdout-repeat`) · Depends on: T30, T31b · Review: quant-auditor.
- [x] **T37: Provider protocol, fake provider, schedule and valuation.** Files: `src/tradepartner/backtest/provider.py` (`DataProvider` protocol with every spec method, all taking `t`), `src/tradepartner/backtest/schedule.py` (rebalance and fill sessions from the calendar), `src/tradepartner/backtest/valuation.py` (`value_positions(positions, marking_frame, fill_session)` pricing dollar positions within one frame; `stitched_returns(frames_by_step)`), `tests/backtest/fake_provider.py` (in-memory provider recording every `t` and every call) · Tests: `tests/backtest/test_schedule.py` (fill session across a holiday and a half day; no weekday logic), `tests/backtest/test_valuation.py` (split inside a month cancels; dividend inside a month reinvested; a name with no bar keeps its last mark; stitched returns reproduce the per-step ratios; AST literal check on `valuation.py`) · Depends on: T30 · Review: quant-auditor.
- [x] **T37b: Engine loop, fills, costs and cash.** Files: `src/tradepartner/backtest/fills.py` (`apply_trades`: fills at the configured price, buys sized after costs, missing fills for buys and sells), `src/tradepartner/backtest/engine.py` (`run(params, provider, start, end, handle, cost_levels) -> dict[level, BacktestResult]`: one read set per step shared by every cost level; signal, targets, fills, valuation; daily equity, cash, weights and rebalance rows; `BacktestResult` exposing marking frames, targets and stitched returns; refuses to run without a `TrialHandle`) · Tests: `tests/backtest/test_engine.py` on the fake provider (only rebalance closes requested; one read set for all levels; `close` vs `open` fills give identical targets and different fill prices; late-known dividend enters the right step or is counted late; cash identity and cash ≥ 0 every session at every level; buy and sell missing fills counted and the unsold name keeps its mark; no handle raises before any call; a plain integer rejected; AST literal check on `fills.py` and `engine.py`) · Depends on: T31b, T33, T34, T37 · Review: quant-auditor.
- [ ] **T37c: Engine exits and benchmarks.** Files: `src/tradepartner/backtest/engine.py` (delisting exit at last close on the final session, paying costs; stale exit; transfer handling per the delistings read; benchmark buy-and-hold series with one initial cost; rebalance-row counters incl. static listings, dropped and late dividends) · Tests: `tests/backtest/test_engine_exits.py` on the fake provider (delisting exit counted and charged; stale exit only for a trailing gap at read time; a gap that closes before T_{i+1} does not exit; benchmarks match hand-computed total return under both fill settings) · Depends on: T37b · Review: quant-auditor.
- [ ] **T38: Store-backed provider.** Files: `src/tradepartner/backtest/store_provider.py` (`StoreProvider(connect, handle, frozen_settings, registry_connect=None)`: `connect` yields a short-lived read-only connection per step and is closed before the next; `registry_connect` (default `connect`) is used once at construction to verify the handle's id in `trials`, which is what lets T40 pass a truncated data store and the untruncated fixture store separately; over `store.asof`, `store.master`, `store.delistings`, `universe.universe_as_of`, `gap.survivorship_gap`; passes `frozen_settings` into every call; ids restricted to members, holdings and benchmarks; never an adapter) · Tests: `tests/backtest/test_store_provider.py` on the fixture store (universe at the README probe Ts; `SEC_WINDOW_DELIST` listing end; `SEC_TRANSFER` delisted between filing and new listing; benchmark ids from `securities.benchmark`; gap values equal `survivorship_gap`; static-listing, dropped- and late-dividend counts; a frozen `universe.top_n_by_cap` changes membership while the live setting does not; one connection per step, closed between steps, a write lock between steps harmless; unknown handle refused; the one AST test that nothing under `backtest/` imports `tradepartner.adapters`) · Depends on: T8b, T13, T15, T31b, T37 · Review: quant-auditor. Needs only T37's protocol and T31b's handle, so it is not serialised behind the engine loop. Near the budget: if over, split the dividend and static-listing reads into a `T38b` by amendment.
- [ ] **T39b: Results, metrics and deflated Sharpe writes.** Files: `src/tradepartner/backtest/results.py` (`write_results(handle, results, family_sharpes)`: metrics per series and level with the req 7 keys, `*_excess_spy` null for SPY; N and, per basis, V, SR*, PSR(0) and DSR from `family_sharpes`, the excess basis from the excess skew and kurtosis; gap maxima; red flag; write metrics, equity per level, weights at base level, rebalances per level, result row), `tests/conftest.py` (add `fixture_store_path`: a temp-file store loaded with `load_universe_fixtures`, for the write/read connection split, shared by `tests/backtest/` and `tests/oracle/`) · Tests: `tests/backtest/test_results.py` on `fixture_store_path` with hand-built `BacktestResult`s from the fake provider (every table written; N and V behave per spec across a rerun, a synthetic run and a holdout run; both bases stored with their own inputs; sensitivity metrics per level; weights base-only; red flag) · Depends on: T31b, T32, T37c · Review: quant-auditor.
- [ ] **T39: Run orchestration and refusals.** Files: `src/tradepartner/backtest/run.py` (`run_hypothesis(slug, start, end, flags, synthetic=False, store_path=None) -> (trial_id, results)`, `store_path` defaulting to `settings.store.path` and never set by the CLI: load frozen settings; open trial on a write connection and close it; window, holdout and gap decisions via `holdout.decide`; `StoreProvider` with a per-step read-only factory; engine at every cost level; hand the results to `results.write_results`; `failed` on any exception or on `store_max_ingested_at` changing; `refused_window` and `refused_holdout` with no provider call, `refused_gap` after gap reads only) · Tests: `tests/backtest/test_run.py` on `fixture_store_path` (ok run returns results and leaves an `ok` row; injected provider error leaves `failed`; a row inserted mid-run leaves `failed` with the named message; each refusal leaves its row and makes the expected calls only; frozen params used; `synthetic=True` refused when `store_path` is `settings.store.path` and accepted on the temp file) · Depends on: T31b, T35, T36, T38, T39b · Review: quant-auditor.
- [ ] **T40: No-look-ahead suite for the full run.** Files: `tests/lookahead/test_backtest_invariance.py` (truncation invariance of `engine.run` with `StoreProvider` at every rebalance session of the fixture range: data factory yielding `harness.TruncatedStore.at(close(T_i))`, `registry_connect` the untruncated fixture store, so `harness.py` stays untouched; equity, weights, rebalances compared; prefix invariance against the full-range run; a synthetic store with a bar revision and a dividend revision known after T_i: run to T_i unchanged, run to T_{i+1} changed) · Tests: the file itself · Depends on: T38 · Review: quant-auditor.
- [ ] **T41: `bt` oracle.** Files: `tests/oracle/{__init__,test_bt_oracle}.py` (register a synthetic `family=oracle` hypothesis on `fixture_store_path` from the root `tests/conftest.py`; call `run_hypothesis(..., store_path=fixture_store_path)` and take `BacktestResult`'s marking frames and targets from its return value; build the stitched frame; `bt` strategy from that frame and the lagged targets, `integer_positions=False`, zero costs; fill-price handling per spec req 14; forced exit of the delisted name at its last close in both engines, asserted; tolerance constant `1e-9`, changed only with review; a deliberate one-session mismatch fails) · Tests: the file itself · Depends on: T39 · Review: quant-auditor.
- [ ] **T42: CLI: `backtest`, `hypothesis register`, `trials`, `decision`.** Files: `src/tradepartner/cli.py` (four commands; exit codes 0/1/2; metrics table; flags per spec req 16 incl. `--holdout-repeat`; no `--synthetic` and no store-path option, so `run_hypothesis` always runs on `settings.store.path`; `--spend-holdout` without `--holdout-reason` refused) · Tests: `tests/test_cli_backtest.py` (exit codes for ok, failed and each refusal; trial id printed; `trials` hides synthetic by default and lists `unfinished`; `decision gap-signoff` appends a row; output passes the `cli_record` scrub patterns) · Depends on: T19, T39 · Review: safety-reviewer (no secrets in output), quant-auditor.
- [x] **T43: Backtest page.** Files: `src/tradepartner/dashboard/backtest_page.py` (trial picker; equity on a log axis for the three series; drawdowns; turnover and cost bars; metrics per cost level; both DSR bases with N, V and SR*, stored and recomputed with today's N and V (a fresh `family_sharpes` read) via `metrics.deflated_sharpe`, labelled "N at run time"; gap, universe size, static reliance and late dividends per rebalance; kind, synthetic, repeat, red and refused states; family holdout spends; "registry not initialised" on a version-1 store), `src/tradepartner/dashboard/app.py` (navigation entry only) · Tests: `tests/dashboard/test_backtest_page.py` (headless render on a store seeded with synthetic registry rows shows every element incl. the recomputed DSR; reads only through the shell's connection; empty-registry and not-initialised states) · Depends on: T21a, T31b · Review: `/code-review`. Phase 2 T21 also adds one line to `_PAGES` in `app.py`; that one-line merge conflict is accepted rather than making T43 wait for T21. Near the budget: if over, split the per-rebalance panels (gap, static reliance, late dividends) into a `T43b` by amendment.
- [ ] **T44: Trial-registry view.** Files: `src/tradepartner/dashboard/trials_page.py` (all trials newest first; hypothesis filter; synthetic hidden by default; failed, refused and unfinished rows with their message; owner decisions; not-initialised state), `src/tradepartner/dashboard/app.py` (navigation entry only) · Tests: `tests/dashboard/test_trials_page.py` (ordering, filter, synthetic toggle, unfinished and refused rows, decisions, not-initialised state) · Depends on: T43 · Review: `/code-review`. Serialised after T43 so the two Phase 3 `app.py` edits never race.
- [ ] **T45: `backtest-runner` agent and docs close-out.** Files: `.claude/agents/backtest-runner.md` (runs a registered hypothesis **only on a temp-file or fixture store**, always passing `store_path`, so its trials are synthetic and never enter or escape the owner's registry; reports; never passes a holdout or override flag; never enters the main checkout), `docs/ways-of-working/agents.md`, this plan (verification section refreshed) · Tests: none new; evidence = `uv run pytest` green on a clean checkout with no `.env` and no network, including the invariance suite and the oracle · Depends on: T29, T40, T41, T42, T44 · Review: quant-auditor.
- [ ] **T45b (owner): Real-store run of H1 and phase close.** Files: `docs/STATUS.md` and `CHANGELOG.md` (the phase-close fold by `doc-keeper`, `--allow-shared-files`), this plan (ticks) · Tests: none; evidence = H1 registered and run by the owner on the real store over the in-sample window under the source T29 names, with the trial row, metrics, both DSR bases, gap and universe size per rebalance and page screenshot in the PR, `quant-auditor` passed on the run, and `trials` showing no `kind=holdout` row (or the logged owner decision if the gate passed) · Depends on: T29, T35b, T45 · Review: quant-auditor on the run. Only the owner's window claims it (`--owner-task`); the store lives in the main checkout, which agents never enter.

**Parallel lanes** (separate worktrees, non-overlapping files): T29 alone; T30 → {T32, T33, T34, T37}; T31 → T31b → {T35, T36, T43}; T35 → T35b; {T31b, T33, T34, T37} → T37b → T37c; {T8b, T13, T15, T31b, T37} → T38 → T40; {T31b, T32, T37c} → T39b; {T31b, T35, T36, T38, T39b} → T39 → {T41, T42}; T19 → T42; T43 → T44; {T29, T40, T41, T42, T44} → T45; {T29, T35b, T45} → T45b.

## Chains (for team claims)

Dependent tasks one team should keep, in order. A chain is a preference, not a lock: every task is still claimed one at a time with `scripts/team.py claim` ([teams.md](../ways-of-working/teams.md)). The `pure` and `registry` chains open the moment this plan merges and run alongside Phase 2; the `store` chain waits for the Phase 2 `rules` chain (T13, T15) and `master` chain (T8b); `cli` also waits for the Phase 2 `pipeline` chain (T19). Merge chain heads first.

| Chain | Tasks | Starts when |
|---|---|---|
| vendor (owner) | T29 | this plan merges; due at Phase 3 start (ADR 0003 rule 8) |
| pure | T30 → T32 → T33 → T34 → T37 | this plan merges (T1 is done) |
| registry | T31 → T31b → T35 → T36 | this plan merges (T4 is done; T35 and T36 also need T30) |
| hypothesis (docs) | T35b | T35 merged; the owner answers spec open questions 1, 2, 8 and 10 on its issue |
| engine | T37b → T37c → T39b | T31b, T33, T34 and T37 merged (T39b also needs T32) |
| store | T38 → T39 | T31b and T37 merged, plus T8b, T13, T15 from Phase 2 (T39 also waits for T35, T36 and T39b) |
| checks | T40, T41 (parallel) | T38 merged for T40; T39 merged for T41 |
| cli | T42 | T19 (Phase 2) and T39 merged |
| ux | T43 → T44 | T31b merged (T21a is done) |
| close-out | T45 → T45b (owner) | every other chain merged, T29 and T35b included |
| fixes | open `size:S` issues with no `team:` label | any time |

## Verification

End to end:
1. Any checkout (T45): `uv run pytest` green with no `.env` and no network, including `tests/lookahead/test_backtest_invariance.py` and `tests/oracle/test_bt_oracle.py`.
2. Owner, main checkout (T45b, after T35b merged): `uv run tradepartner hypothesis register docs/hypotheses/h1-momentum-12-1.md` prints the hypothesis id, the full frozen set and its hash; the real store is migrated to schema version 2 by that command.
3. Owner: `uv run tradepartner backtest h1-momentum-12-1` exits 0, prints the trial id, the metrics per cost level, both DSR bases with N and V, and the gap maxima; a direct DuckDB query of `trials`, `trial_results` and `trial_rebalances` is pasted alongside.
4. Owner: `uv run tradepartner backtest h1-momentum-12-1 --end <a date inside the holdout>` exits 2 and `trials` shows the `refused_holdout` row; `--end <a date after holdout.end>` exits 2 with `refused_window`; `HOLDOUT__START=2030-01-01` in the environment changes nothing.
5. Owner: `uv run tradepartner dashboard` shows the trial on the backtest page (stored and recomputed DSR) and in the trial-registry view (screenshots).
6. `quant-auditor` report on the H1 run attached (T45b).

## Rollback

Registry tables are additive: the fact tables are never touched. To roll back T31 on a store that already reached version 2: drop the eight `REGISTRY_TABLE_NAMES` tables, then `DELETE FROM schema_version WHERE version = 2` by hand, because a reverted `init_schema` raises `SchemaVersionError` on any version other than 1 (`store/schema.py`); write that command in the revert PR. Dependencies are removed by reverting T30. Hypothesis files are docs. No scheduled job is added in this phase.
