# Plan: Backtest engine and trial registry (Phase 3)

**Spec:** [specs/backtest.md](../specs/backtest.md)  ·  **Status:** Draft

## Approach (short)

Build the pure parts first, then the store-facing parts, then the checks, then the surfaces. Config and dependencies; the registry schema and its append-only API; metrics with deflated Sharpe, the cost model, the signal and portfolio rules, the holdout decision: all pure functions over synthetic inputs, none of which needs Phase 2 to finish. The engine is written against a `DataProvider` protocol with a fake provider in tests, so it too starts early. Only the store-backed provider needs `universe_as_of` (T13), `survivorship_gap` (T15) and read-time delistings (T8b); everything after it (run orchestration, invariance suite, oracle, CLI) waits on that one task. The two pages read registry tables and start as soon as the registry API merges.

**What may start while Phase 2 runs.** Any task whose dependencies are all ticked on `origin/main` is claimable the moment this plan merges (teams.md): T30 (depends on T1) and T31 (on T4) immediately; T31b, T32, T33, T34 next; T35, T36, T43 once T31b and T30 merge; T37 once T33 and T34 merge. T38 is the gate to the Phase 2 chain; T42 also waits on the Phase 2 CLI (T19). Starting early is deliberate: pure functions over synthetic inputs test the same whether or not real data exists, and the engine's correctness does not depend on the vendor decision. Spec open question 9 puts that choice to the owner; merging this plan records it.

**Price-vendor ADR.** No task depends on it and none writes a vendor adapter (out of scope). The close-out (T45) and the phase exit are gated on it: the owner confirms on T45's issue that the ADR is merged before T45 is claimed. If the ADR chooses a vendor, its adapter is a new plan task added by amendment before T45 runs.

Design choices: engine as a rebalance loop over a `t`-parameterised provider, positions carried as dollar values priced within one adjusted-as-of-`t` frame per step ([ADR 0003](../decisions/0003-data-adapters-local-first.md) rule 2, [ADR 0006](../decisions/0006-universe-and-cadence.md)); our own engine with `bt` as a test-only oracle over a stitched frame, at a tolerance pinned in the test file ([ADR 0004](../decisions/0004-tooling-adopt-avoid.md); the roadmap's "configured tolerance" means that constant, not a `Settings` key); registry tables in the DuckDB store, append-only, trial handle before data ([ADR 0005](../decisions/0005-objective-benchmark-stop-criteria.md)); frozen parameters per hypothesis including the holdout window and gap threshold; deflated Sharpe hand-written and tested against reference values. **Metrics library:** ADR 0004 leaves the choice between `empyrical-reloaded` and `quantstats` to this plan. This plan picks **`empyrical-reloaded`** (0.5.12, pinned): pure functions over series with no plotting stack; on PyPI (checked 2026-09-25) its runtime requirements are numpy, pandas, scipy, bottleneck and peewee, with `yfinance` only as an optional extra, whereas `quantstats` requires `yfinance`, matplotlib and seaborn outright and ADR 0004 avoids `yfinance` outside spikes. `bt==1.2.3` goes in the dev dependency group only; it pulls `ffn`, which pulls `yfinance` and `scikit-learn`, into the dev environment and nowhere else. An AST test keeps `bt`, `ffn` and `yfinance` out of `src/`, and the autouse no-network fixture keeps them off the network.

**All dependencies are added in T30** (`empyrical-reloaded` runtime, exact pin; `bt` dev, exact pin) so no later task touches `pyproject.toml` or `uv.lock`. T30 also appends a dated amendment note to ADR 0004 recording the metrics choice and the oracle's fill-session-only comparison under `open` fills.

**Phase 2 interface note.** T38 passes the trial's frozen `Settings` into every as-of call. `adjusted_prices_as_of` already takes `settings`; `universe_as_of` (T13) and `survivorship_gap` (T15) must accept one too. If they merge without it, a `size:S` issue adds the parameter before T38 starts.

Package layout: `src/tradepartner/backtest/{__init__,metrics,costs,signals,portfolio,hypothesis,holdout,provider,engine,store_provider,run}.py`, `src/tradepartner/store/registry.py`, `src/tradepartner/dashboard/{backtest_page,trials_page}.py`, `docs/hypotheses/`, `docs/templates/hypothesis.md`; tests under `tests/backtest/`, `tests/lookahead/test_backtest_invariance.py`, `tests/oracle/test_bt_oracle.py`, `tests/dashboard/`.

## Tasks

Each task = one branch = one PR (~≤400 lines). Tasks with no shared files may run in parallel. Task ids start at T30 because ids are global across plans.

- [ ] **T30: Phase 3 config and dependencies.** Files: `src/tradepartner/config.py` (`HypothesesConfig` with the enumerated `families`, `StrategyConfig`, `CostsConfig`, `HoldoutConfig`, `BacktestConfig`, `MetricsConfig` with every spec key and default; `holdout.end` after `start`; sensitivity list non-negative), `pyproject.toml`, `uv.lock` (`empyrical-reloaded==0.5.12` runtime; `bt==1.2.3` in the dev group; mypy `ignore_missing_imports` for `empyrical`), `docs/decisions/0004-tooling-adopt-avoid.md` (append "Amendment 2026-xx-xx": metrics library chosen and why, `bt` dev-only with its transitive `yfinance`, oracle compares fill sessions only under `open` fills) · Tests: `tests/test_config.py` (every new key has its documented default; invalid holdout window, negative sensitivity and a family outside the list rejected; no `.env` still loads; `.env.example` names no `HOLDOUT__` key), `tests/test_no_forbidden_imports.py` (AST: nothing under `src/` imports `bt`, `ffn` or `yfinance`); PR attaches `uv tree` for the runtime closure · Depends on: T1 · Review: quant-auditor.
- [ ] **T31: Registry schema and migration.** Files: `src/tradepartner/store/schema.py` (eight registry tables in `REGISTRY_TABLE_NAMES`, disjoint from `TABLE_NAMES`; `CURRENT_SCHEMA_VERSION = 2`; additive migration: a version-1 store gets the new tables and an appended version row, nothing else changes; a read-only connection reports `RegistryNotInitialised` instead of migrating) · Tests: `tests/store/test_registry_schema.py` (tables and version after a fresh init and after opening a version-1 store; fact tables byte-identical after migration; read-only open of a version-1 store raises the named error; `REGISTRY_TABLE_NAMES ∩ TABLE_NAMES = ∅`; `tests/lookahead/test_asof_invariance.py` still passes unchanged) · Depends on: T4 · Review: quant-auditor.
- [ ] **T31b: Registry API and trial handle.** Files: `src/tradepartner/store/registry.py` (`TrialHandle` constructed only by `open_trial`; `register_hypothesis`, `open_trial`, `close_trial`, `write_result`, `write_metrics`, `write_equity`, `write_weights`, `write_rebalances`, `record_decision`, `family_sharpes`, `family_holdout_spends`, `list_trials` with `unfinished` derived; inserts and reads only; `code_version` from `git rev-parse HEAD` with a dirty flag, `unknown` outside a checkout; `store_max_ingested_at` captured at open), `docs/research/trial-registry.md` (now a pointer to the tables and the `trials` command) · Tests: `tests/store/test_registry.py` (no `UPDATE`/`DELETE` in the module text; ids increase; closing twice raises; a handle cannot be built outside `open_trial`; `family_sharpes` returns N over all `ok` non-synthetic `in_sample` rows and V over distinct (parameter hash, window) pairs; `family_holdout_spends`; `unfinished` listing; decision rows; code version and dirty flag) · Depends on: T31 · Review: quant-auditor.
- [ ] **T32: Metrics and deflated Sharpe.** Files: `src/tradepartner/backtest/{__init__,metrics}.py` (CAGR, annualized volatility and Sharpe from monthly returns via `empyrical`, maximum drawdown on daily equity, one-sided turnover, cost drag, excess CAGR and tracking error versus a benchmark, skew and kurtosis, `expected_max_sharpe`, `probabilistic_sharpe`, `deflated_sharpe` on raw and on excess-over-SPY returns with the `psr` basis when fewer than two distinct pairs, `red_flag`; γ at full precision) · Tests: `tests/backtest/test_metrics.py` (hand-computed values on a synthetic series; the spec's DSR reference values to 1e-6; N=1 reduces to PSR; monotonicity in N, kurtosis and negative skew; red flag either side of the threshold; AST literal check on `metrics.py`) · Depends on: T30 · Review: quant-auditor.
- [ ] **T33: Cost model.** Files: `src/tradepartner/backtest/costs.py` (`trade_cost(notional, shares, per_side_bps, commissions)`, `buy_notional_after_costs(cash, per_side_bps, commissions)` so cash never goes negative, `sensitivity_levels(settings)`) · Tests: `tests/backtest/test_costs.py` (three-trade fixture against hand-computed values per component, ADR 0004; zero cost is the identity; post-cost cash ≥ 0 at the ladder maximum; levels sorted and include the base; AST literal check on `costs.py`) · Depends on: T30 · Review: quant-auditor.
- [ ] **T34: Momentum signal and portfolio construction.** Files: `src/tradepartner/backtest/signals.py` (`momentum_12_1(frame, t_session, formation_months, skip_months)` over an adjusted price frame and the calendar's month-end sessions; excluded names reported), `src/tradepartner/backtest/portfolio.py` (`target_weights(scores, top_fraction, weighting)`: long-only, ≥ 0, sum ≤ 1, ties by `security_id` ascending; `drifted_weights`, `trades_from(target, drifted)`) · Tests: `tests/backtest/test_signals.py` (skip month excluded; missing formation bar excluded and counted; a session after `t_session` in the frame is never read), `tests/backtest/test_portfolio.py` (weights properties; tie order; empty score set gives all cash) · Depends on: T30 · Review: quant-auditor.
- [ ] **T35: Hypothesis files and pre-registration.** Files: `docs/templates/hypothesis.md`, `docs/hypotheses/h1-momentum-12-1.md` (rationale from handoff H1 and G1's primary sources; every `strategy.*` and `costs.*` key named; `in_sample_start` per spec open question 8; expected magnitudes and red flags; retirement condition), `docs/README.md` (one row for `hypotheses/`), `src/tradepartner/backtest/hypothesis.py` (parse the file's parameter block; frozen set = file values for named keys, live `Settings` for the rest of the spec's frozen list; SHA-256 of file and parameters; `register` through `store.registry`; `load_frozen(slug) -> Settings`) · Tests: `tests/backtest/test_hypothesis.py` (H1 file parses; frozen set is exactly the spec's key list; file values win over `Settings`; changed file gives a new hypothesis; frozen values survive an environment override of `HOLDOUT__START`, `GAP__COUNT_SHARE_THRESHOLD` and `STRATEGY__TOP_FRACTION`; family outside the list refused; unregistered slug refused) · Depends on: T30, T31b · Review: quant-auditor; spec-critic on the H1 file.
- [ ] **T36: Holdout and gap-gate decisions.** Files: `src/tradepartner/backtest/holdout.py` (`window_touches_holdout`, `default_in_sample_end`, `decide(window, frozen, flags, reasons, gap_series, prior_spends) -> Decision` with `run`, `refused_holdout`, `refused_gap`, the `holdout_repeat` marker; pure) · Tests: `tests/backtest/test_holdout.py` (overlap at either edge; default in-sample end is the last rebalance session before the frozen `holdout.start`; flag without reason refused; gap above the frozen threshold at one date refused unless overridden; a prior family spend marks repeat) · Depends on: T30, T31b · Review: quant-auditor.
- [ ] **T37: Engine core: loop, valuation, fills, cash.** Files: `src/tradepartner/backtest/provider.py` (`DataProvider` protocol; every method takes `t`), `src/tradepartner/backtest/engine.py` (`run(params, provider, start, end, handle, cost_levels) -> dict[level, BacktestResult]`: rebalance sessions from the calendar; one read set per step shared by every cost level; positions as dollar values priced within the step's marking frame; fills at F_i at the configured price; buys sized after costs; missing fills to cash; daily equity, weights and rebalance rows; refuses to run without a `TrialHandle`), `tests/backtest/fake_provider.py` (in-memory provider that records every `t` and every call) · Tests: `tests/backtest/test_engine.py` (only rebalance closes requested; one read set for all levels; fill session across a holiday and a half day; `close` vs `open` fills give identical targets and different fill prices; split and dividend inside a month valued as total return; late-known dividend enters the right step; cash identity and cash ≥ 0 every session at every level; missing fill to cash; no handle raises before any call) · Depends on: T33, T34 · Review: quant-auditor.
- [ ] **T37b: Engine exits and benchmarks.** Files: `src/tradepartner/backtest/engine.py` (delisting exit at last close on the final session; stale exit; transfer handling per the delistings read; benchmark buy-and-hold series with one initial cost; rebalance-row counters incl. static listings and dropped dividends) · Tests: `tests/backtest/test_engine_exits.py` on the fake provider (delisting exit counted; stale exit only for a trailing gap at read time; a gap that closes before T_{i+1} does not exit; benchmarks match hand-computed total return under both fill settings) · Depends on: T37 · Review: quant-auditor.
- [ ] **T38: Store-backed provider.** Files: `src/tradepartner/backtest/store_provider.py` (`StoreProvider(conn, handle, frozen_settings)` over `store.asof`, `store.master`, `store.delistings`, `universe.universe_as_of`, `gap.survivorship_gap`; refuses a handle absent from the store's `trials`; passes `frozen_settings` into every call; ids restricted to members, holdings and benchmarks; never an adapter) · Tests: `tests/backtest/test_store_provider.py` on the fixture store (universe at the README probe Ts; `SEC_WINDOW_DELIST` listing end; `SEC_TRANSFER` delisted between filing and new listing; benchmark ids from `securities.benchmark`; gap values equal `survivorship_gap`; static-listing count; dropped-dividend count; a frozen `universe.top_n_by_cap` changes membership while the live setting does not; unknown handle refused; AST: no `tradepartner.adapters` import under `backtest/`) · Depends on: T8b, T13, T15, T37b · Review: quant-auditor.
- [ ] **T39: Run orchestration, sensitivities and results.** Files: `src/tradepartner/backtest/run.py` (`run_hypothesis(slug, start, end, flags, synthetic=False) -> trial_id`: load frozen settings; open trial on a write connection and close it; holdout decision; provider on a read-only connection; engine at every cost level; metrics per series and level; N, V, SR*, both DSRs from `family_sharpes`; gap maxima; red flag; write results on a fresh write connection; `failed` on any exception or on `store_max_ingested_at` changing; `refused_holdout` with no provider call; `refused_gap` after gap reads only) · Tests: `tests/backtest/test_run.py` on the fixture store (ok run writes every table; injected provider error leaves `failed`; a row inserted mid-run leaves `failed` with the named message; refusals leave rows and the expected calls; N and V behave per spec across a rerun, a synthetic run and a holdout run; sensitivity metrics per level; frozen params used) · Depends on: T31b, T32, T35, T36, T38 · Review: quant-auditor.
- [ ] **T40: No-look-ahead suite for the full run.** Files: `tests/lookahead/test_backtest_invariance.py` (truncation invariance of `engine.run` with `StoreProvider` at every rebalance session of the fixture range using `harness.TruncatedStore`: equity, weights, rebalances; prefix invariance against the full-range run; a synthetic store with a bar revision and a dividend revision known after T_i: run to T_i unchanged, run to T_{i+1} changed) · Tests: the file itself · Depends on: T38 · Review: quant-auditor.
- [ ] **T41: `bt` oracle.** Files: `tests/oracle/{__init__,test_bt_oracle}.py` (register a synthetic `family=oracle` hypothesis in the in-memory store; build the stitched frame from the engine's marking frames; `bt` strategy from that frame and the lagged target weights, `integer_positions=False`, zero costs; fill-price handling per spec req 14; forced exit of the delisted name at its last close in both engines, asserted; tolerance constant `1e-9`, changed only with review; the run logged as a synthetic trial through `run.py`; a deliberate one-session mismatch fails) · Tests: the file itself · Depends on: T39 · Review: quant-auditor.
- [ ] **T42: CLI: `backtest`, `hypothesis register`, `trials`, `decision`.** Files: `src/tradepartner/cli.py` (four commands; exit codes 0/1/2; metrics table; flags per spec req 16; no `--synthetic`; `--spend-holdout` without `--holdout-reason` refused) · Tests: `tests/test_cli_backtest.py` (exit codes for ok, failed and refused; trial id printed; `trials` hides synthetic by default and lists `unfinished`; `decision gap-signoff` appends a row; output passes the `cli_record` scrub patterns) · Depends on: T19, T39 · Review: safety-reviewer (no secrets in output), quant-auditor.
- [ ] **T43: Backtest page.** Files: `src/tradepartner/dashboard/backtest_page.py` (trial picker; equity on a log axis for the three series; drawdowns; turnover and cost bars; metrics per cost level; both DSRs with N, V and SR*; gap, universe size and static reliance per rebalance; kind, synthetic, repeat, red and refused states; family holdout spends; "registry not initialised" on a version-1 store), `src/tradepartner/dashboard/app.py` (navigation entry only) · Tests: `tests/dashboard/test_backtest_page.py` (headless render on a store seeded with synthetic registry rows shows every element; reads only through the shell's connection; empty-registry and not-initialised states) · Depends on: T21a, T31b · Review: `/code-review`. If Phase 2 T21's PR is open on `app.py`, wait for it (teams.md).
- [ ] **T44: Trial-registry view.** Files: `src/tradepartner/dashboard/trials_page.py` (all trials newest first; hypothesis filter; synthetic hidden by default; failed, refused and unfinished rows with their message; owner decisions; not-initialised state), `src/tradepartner/dashboard/app.py` (navigation entry only) · Tests: `tests/dashboard/test_trials_page.py` (ordering, filter, synthetic toggle, unfinished and refused rows, decisions, not-initialised state) · Depends on: T43 · Review: `/code-review`. Serialised after T43 so the two `app.py` edits never race.
- [ ] **T45: `backtest-runner` agent and close-out.** Files: `.claude/agents/backtest-runner.md` (runs a registered hypothesis, reports, never passes a holdout or override flag), `docs/ways-of-working/agents.md`, `docs/STATUS.md` and `CHANGELOG.md` (the phase-close fold by `doc-keeper`, `--allow-shared-files`), this plan · Tests: none new; evidence = the price-vendor ADR merged (confirmed on the issue before claiming), H1 registered and run on the owner's real store over the in-sample window under the source that ADR names, with the trial row, metrics, both DSRs, gap and universe size per rebalance and page screenshot in the PR, `quant-auditor` passed on the run, and `trials` showing no `kind=holdout` row (or the logged owner decision if the gate passed) · Depends on: T40, T41, T42, T44 · Review: quant-auditor on the run.

**Parallel lanes** (separate worktrees, non-overlapping files): T30 → {T32, T33, T34}; T31 → T31b → {T35, T36, T43}; {T33, T34} → T37 → T37b; {T8b, T13, T15, T37b} → T38 → T40; {T31b, T32, T35, T36, T38} → T39 → {T41, T42}; T19 → T42; T43 → T44; {T40, T41, T42, T44} → T45.

## Chains (for team claims)

Dependent tasks one team should keep, in order. A chain is a preference, not a lock: every task is still claimed one at a time with `scripts/team.py claim` ([teams.md](../ways-of-working/teams.md)). The `pure` and `registry` chains open the moment this plan merges and run alongside Phase 2; the `engine` chain past T37b waits for the Phase 2 `rules` chain (T13, T15) and `master` chain (T8b); `cli` also waits for the Phase 2 `pipeline` chain (T19). Merge chain heads first.

| Chain | Tasks | Starts when |
|---|---|---|
| pure | T30 → T32 → T33 → T34 | this plan merges (T1 is done) |
| registry | T31 → T31b → T35 → T36 | this plan merges (T4 is done; T35 and T36 also need T30) |
| engine | T37 → T37b → T38 → T39 | T33 and T34 merged (T38 also waits for T8b, T13, T15 from Phase 2) |
| checks | T40, T41 (parallel) | T38 merged for T40; T39 merged for T41 |
| cli | T42 | T19 (Phase 2) and T39 merged |
| ux | T43 → T44 | T31b merged (T21a is done) |
| close-out | T45 | every other chain merged; the price-vendor ADR merged |
| fixes | open `size:S` issues with no `team:` label | any time |

## Verification

End to end, on a clean checkout:
1. `uv run pytest` green with no `.env` and no network, including `tests/lookahead/test_backtest_invariance.py` and `tests/oracle/test_bt_oracle.py`.
2. `uv run tradepartner hypothesis register docs/hypotheses/h1-momentum-12-1.md` prints the hypothesis id, the full frozen set and its hash; the owner's store is migrated to schema version 2 by that command.
3. With the owner's store: `uv run tradepartner backtest h1-momentum-12-1` exits 0, prints the trial id, the metrics per cost level, both DSRs with N and V, and the gap maxima; a direct DuckDB query of `trials`, `trial_results` and `trial_rebalances` is pasted alongside.
4. `uv run tradepartner backtest h1-momentum-12-1 --end <a date inside the holdout>` exits 2 and `trials` shows the `refused_holdout` row; `HOLDOUT__START=2030-01-01` in the environment changes nothing.
5. `uv run tradepartner dashboard` shows the trial on the backtest page and in the trial-registry view (screenshots).
6. `quant-auditor` report on the H1 run attached (T45).

## Rollback

Registry tables are additive: a store at schema version 2 with the eight registry tables dropped and the version-2 row removed is a valid version-1 store; the fact tables are never touched. Dependencies are removed by reverting T30. Hypothesis files are docs. No scheduled job is added in this phase.
