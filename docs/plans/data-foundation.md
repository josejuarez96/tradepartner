# Plan: Data foundation (Phase 2)

**Spec:** [specs/data-foundation.md](../specs/data-foundation.md)  ·  **Status:** Draft

## Approach (short)

Build inside-out (the filing index is always scanned over full history; `--since` limits only bars, actions and facts): config and calendar; raw-fetch clients and the recorder (so the owner can record fixtures early); the store and its as-of API with the truncation-invariance harness; the fixture universe; fixture adapters and the security master; the no-look-ahead suite; parsers for the real sources over the owner's recordings; universe, gap, ingest, health, CLI, page, close-out. CI never touches the network: an autouse fixture disables sockets.

Design choices: DuckDB file, raw OHLCV, read-time adjustment ([ADR 0003](../decisions/0003-data-adapters-local-first.md), [0004](../decisions/0004-tooling-adopt-avoid.md)); universe rules as config with a guarded SIC setting ([ADR 0006](../decisions/0006-universe-and-cadence.md)); **Streamlit** for the page; **Typer** for the CLI; **httpx** for raw EDGAR fetches; `edgartools` only for cover-page iXBRL parsing. Typer, pydantic-settings and httpx are recorded here as additions to ADR 0004's adopt table via a dated amendment note appended in T1 (ADRs are append-only).

**All runtime dependencies are added in T1** so no later task touches `pyproject.toml`/`uv.lock` (except T19's `[project.scripts]`): `pydantic`, `pydantic-settings`, `exchange_calendars`, `typer`, `httpx`, `duckdb`, `pyarrow`, `polars`, `edgartools` (exact pin), `alpaca-py` (exact pin), `streamlit`. Per-module `ignore_missing_imports` set in T1.

Package layout: `src/tradepartner/{config.py, calendar.py, cli_record.py, store/{schema,db,asof,master,classify}.py, adapters/{__init__,prices,filings,broker,fixture_prices,fixture_filings,fake_broker,alpaca_raw,edgar_raw,alpaca_prices,edgar,edgar_source}.py, universe.py, gap.py, health.py, ingest.py, backfill.py, cli.py, dashboard/{__init__,app,health_page}.py}`; `tests/conftest.py` owns the fixture-store loader and the no-network fixture.

**Amendment 2026-09-25 (#216).** #174 found that the SEC Financial Statement and Notes data sets carry the cover facts and a per-filing SIC keyed by accession, so the cover-page scope of T11c is rebuilt on those files: T11c fetches and parses them into unstamped per-CIK caches, T11d serves cover pages and SIC headers from them, stamped at read time from the submissions acceptance, with per-filing downloads only for accessions FSN does not yet hold and ranged SGML headers only for `edgar.header_forms` filings absent from FSN, and moves the classification call site from `master.issuer_forms` to `edgar.header_forms`; T11e serves facts; T11f holds delistings, the failure policy for a filing that fails the same way on every run, and the owner's backfill measurement. Each is one team's work; T19 depends on T11f and its `health --check` warns on a quarantined accession. Merging this amendment records owner decisions (1) (in T11c) and (2) (in T11f), and appends a dated note to the spec's security-master table.

**Amendment 2026-09-25 (#163).** No task built the EDGAR `FilingSource` adapter that spec req 6 requires: T2 delivered the raw client and T11 the parsers, and T16 is tested with fixture sources, so a real `tradepartner ingest` had no filing source. T11b and T11c add it in the parsers chain; T19 now depends on T11c (superseded by #216: T11f). Scale rules the design. The full index lists every filer (about a million CIKs), so the adapter keeps only issuer CIKs. T16 rebuilds full views on every run, so every run needs acceptance times for every kept row: stamped rows are therefore cached by accession and each run stamps only the accessions it has not seen, from the nightly `submissions.zip` when they belong to more than a threshold of CIKs (the backfill, or a long gap), per CIK otherwise, with a per-CIK top-up after a bulk pass because the zip is rebuilt at about 03:00 ET and never has that day's filings ([research](../research/2026-09-25-free-data-terms.md), runtime estimate). Cover pages and SGML headers for every issuer filing are hundreds of thousands of downloads; #174 (research) decides how both are sourced before T11c's clauses for them are built. T16 is unchanged but holds the write lock through its fetch pass, which #173 moves before T22; #172 writes the adapter's counts into the run row before T19. `scripts/team.py` reads only `T..` ids in "Depends on", so the issue gates written on T11c, T19 and T22 are not enforced by the ready list: read them before claiming.

## Tasks

Each task = one branch = one PR (~≤400 lines; generated fixture CSVs excluded). Tasks with no shared files may run in parallel. "Owner" tasks need the owner's keys and are not run by `implementer`.

- [x] **T1: Config, dependencies, calendar.** Files: `src/tradepartner/{config,calendar}.py`, `pyproject.toml`, `uv.lock`, `.env.example`, `docs/decisions/0004-tooling-adopt-avoid.md` (append "Amendment 2026-xx-xx" note listing typer, pydantic-settings, httpx) · Tests: `tests/test_config.py` (every key has its default; guarded SIC pinned; `liquidity_rule_enabled` defaults false; `fill_price` defaults close; no `.env` → import and load succeed), `tests/test_calendar.py` (sessions, open/close timestamps, last session of month, last completed session as of a timestamp, month window, half day, holiday; no weekday logic) · Depends on: n/a · Review: quant-auditor.
- [x] **T2: Raw-fetch clients and the recorder.** Files: `src/tradepartner/adapters/{alpaca_raw,edgar_raw}.py` (thin: return raw JSON / SGML text / downloaded files; EDGAR User-Agent and ~10 req/s throttle from config), `src/tradepartner/cli_record.py` (`python -m tradepartner.cli_record`: fetches a fixed list of payloads for a few CIKs and symbols, scrubs, writes to `tests/fixtures/{alpaca,edgar}/`), `tests/test_fixture_scrub.py` (pattern-based: key shapes, emails, User-Agent shapes) · Tests: scrub test on an empty fixture dir passes; a smoke test of the clients is marked `network` and skipped in CI · Depends on: T1 · Review: safety-reviewer.
- [x] **T3 (owner): Record fixtures and resolve source facts.** Files: `tests/fixtures/{alpaca,edgar}/*` (recorded), `docs/research/2026-xx-xx-free-data-terms.md`, `src/tradepartner/config.py` (set `liquidity_rule_enabled`, `execution.fill_price`, `ingest.max_missing_share`), `tests/test_config.py` (update the pinned defaults) · Tests: scrub test passes on the recordings · Depends on: T2 · Review: safety-reviewer. The owner runs the recorder with their keys and records: Alpaca terms and local-storage permission, history depth, open = auction or IEX, fractional-at-open behavior, actions depth, assets-endpoint ticker reuse; EDGAR rate limit; a backfill runtime estimate; and the resulting config values. Runs in parallel with T4–T10.
- [x] **T4: Store schema, db layer, shared test loader.** Files: `src/tradepartner/store/{__init__,schema,db}.py`, `src/tradepartner/adapters/__init__.py`, `tests/conftest.py` (fixture-store loader from CSV; **autouse no-network fixture**) · Tests: `tests/store/test_schema.py` (column and provenance constraints; read-only helper; lock-retry helper; chunk-scoped write connection) · Depends on: T1 · Review: quant-auditor.
- [x] **T5: Fixture universe (CSV) and generator.** Files: `scripts/make_fixture_universe.py`, `tests/fixtures/universe/*.csv`, `tests/fixtures/universe/README.md` · Tests: `tests/test_fixture_universe.py` (every case in spec req 13 present; content-equal regeneration) · Depends on: T4 · Review: quant-auditor.
- [x] **T6: As-of primitives and truncation-invariance harness.** Files: `src/tradepartner/store/asof.py` (`prices_as_of`, `adjusted_prices_as_of(include_dividends)`, `facts_as_of`, `listings_as_of`; bare date raises), `tests/lookahead/{__init__,harness}.py` (truncate helper; probes just before and after every distinct `known_at`) · Tests: `tests/store/test_asof.py` (latest revision; bar revision; split before/after `known_at`; backfilled split; revised dividend; date input raises), `tests/lookahead/test_asof_invariance.py` · Depends on: T5 · Review: quant-auditor.
- [x] **T7: `PriceSource` interface and fixture adapter.** Files: `src/tradepartner/adapters/{prices,fixture_prices}.py` · Tests: `tests/adapters/test_fixture_prices.py` (bars; actions with first-seen proxy and revision rule; resolution by `security_id`) · Depends on: T6 · Review: quant-auditor.
- [x] **T8: `FilingSource` interface, fixture adapter, security master core.** Files: `src/tradepartner/adapters/{filings,fixture_filings}.py`, `src/tradepartner/store/master.py` (`securities_as_of` from earliest issuer filing over full history; listings with class title; `snapshot_static` columns per config; dual-class; benchmark seeding) · Tests: `tests/store/test_master.py` (earliest-filing rule; issuer-forms filter excludes a Form 4-only filer; static reliance; reused ticker; same-company ticker change; dual-class listings; benchmarks seeded) · Depends on: T6 · Review: quant-auditor. Runs in parallel with T7.
- [x] **T8b: Delistings and transfers, derived at read time.** Files: `src/tradepartner/store/delistings.py` (`listings_as_of(..., settings: Settings | None = None)` end derivation, reading `master.transfer_window_sessions` from it (#102); 25/25-NSE per class and exchange; transfer detection from rows known ≤ T) · Tests: `tests/store/test_delistings.py` (preferred-class Form 25 leaves common; 25-NSE; transfer once new listing known; delisted at a T between filing and new listing; clean merger end session; an override `Settings` with a different `master.transfer_window_sessions` changes the transfer verdict) · Depends on: T8 · Review: quant-auditor.
- [x] **T9: Security-type classification.** Files: `src/tradepartner/store/classify.py` (any read-time entry point that reads config takes `settings: Settings | None = None`; #102) · Tests: `tests/store/test_classify.py` (one test per rule incl. F-6; unclassifiable bucket; each row carries rule and the `known_at` of the filing used or fetch time) · Depends on: T8b · Review: quant-auditor.
- [x] **T10: No-look-ahead suite and broken adapter.** Files: `tests/lookahead/{test_suite,broken_adapter}.py` · Tests: suite passes on fixture adapters; broken adapter's five violations each caught by the named check · Depends on: T7, T8b · Review: quant-auditor.
- [x] **T11: EDGAR parsers.** Files: `src/tradepartner/adapters/edgar.py` (parsers over T3's recorded payloads: filing index, companies snapshot, company facts with `as_of_date` and class dimension, SGML header SIC, cover page via `edgartools`, Forms 25/25-NSE class and exchange) · Tests: `tests/adapters/test_edgar.py` (provenance and `known_at` per the master table) · Depends on: T3, T9 · Review: quant-auditor, safety-reviewer.
- [x] **T11b: EDGAR `FilingSource` adapter, index and snapshot.** Files: `src/tradepartner/adapters/edgar_source.py` (`EdgarFilingSource(FilingSource)`, constructed with `settings`, an optional `httpx.Client` and an optional `clock`, so tests inject a `MockTransport` and a fixed time; every fetch goes through `edgar_raw`, so throttle, retry and User-Agent stay there; `.requests` counts the requests made through the client with an `httpx` event hook. `facts`, `filing_headers`, `cover_pages` and `delistings` are defined here and raise `NotImplementedError("T11c")` (superseded by #216: T11d to T11f); T11c to T11f replace them (#216). `filing_index(since)`: fetches the quarterly `form.idx` files from `edgar.index_first_year` to the clock's quarter; a quarter's raw index is cached under `edgar.cache_dir` only when fetched at least `edgar.index_settle_days` after its Eastern-time end, younger quarters are re-fetched every run, and a 404 for the open quarter is an empty index. Keeps the rows of CIKs that have at least one `master.issuer_forms` row in the index other than a Form 25 or 25-NSE (all their forms, since classification needs the others), drops other filers' rows and counts them on `.skipped_filers`. A 25 or 25-NSE row (and their `/A`) is kept only under its subject company: a row is dropped when its CIK is the accession's filer prefix and another CIK lists the same accession; rows are de-duplicated on CIK and accession, so a combined filing stays under each co-registrant. *Amended 2026-09-25 (owner, on quant-auditor's review of #188):* the filer-copy drop applies to 25-NSE and 25-NSE/A only, since an issuer files its own Form 25 and a parent's copy naming a co-registrant must stay; and a CIK named as the subject (not the filer prefix) of a 25 or 25-NSE is an issuer too, so a company seen only through its delisting is kept. Pre-2009 small-business forms (10-KSB, 10-QSB and similar) stay out of `master.issuer_forms`: every company trading from 2016, where the price history starts, files 10-K/10-Q or 20-F/40-F. Stamping: the per-accession cache stores the reduced record (accession, form, `primaryDocument`, `isInlineXBRL`, acceptance) as JSON under `edgar.cache_dir`, keyed by a parser-version constant, so a parser fix re-stamps; a CIK's submissions are fetched only when it has an accession not in the cache; an accession in a settled quarter that is absent from a submissions fetch made after the quarter settled is cached as unstampable, still reported on `.unstamped_filings`, and not re-fetched; every other unstamped row is never cached as final; the accessions not yet stamped are stamped with `acceptance_times` from one streamed download of `submissions.zip` when they belong to more than `edgar.bulk_stamp_threshold_ciks` CIKs, otherwise from per-CIK `submissions` plus `submissions_page`, pages fetched only while accessions stay unstamped; after a bulk pass, CIKs with rows still unstamped get the per-CIK fetch; rows still without an acceptance time are excluded from the result and collected on `.unstamped_filings`, reset per call, never dated from the index. Per-CIK submissions are fetched lazily, memoised for T11c, and reduced to accession, form, `primaryDocument`, `isInlineXBRL` and acceptance. Finally keeps `accepted_at >= since` as the ABC says. `companies_snapshot()` from `company_tickers`, `fetched_at` taken after the response arrives), `src/tradepartner/adapters/edgar_raw.py` (`bulk_submissions()` and `bulk_company_facts()` streaming `submissions.zip` and `companyfacts.zip` into `edgar.cache_dir`; a pure `cached_filing_path(cik, accession, filename)`; `download_filing_file` returns a file already present without a request; every cache write goes to a temp file then `os.replace`), `tests/adapters/test_edgar_raw_offline.py` (a traversal filename still raises when a file already exists at the target, so validation runs before the early return; a present file is returned without a request; the temp file is replaced atomically), `src/tradepartner/adapters/filings.py` (`filing_index` docstring: rows of issuer CIKs, all their forms), `src/tradepartner/config.py` and `tests/test_config.py` (`edgar.index_first_year=1993`, `edgar.index_settle_days=3`, `edgar.bulk_stamp_threshold_ciks=500`, `edgar.cover_page_forms=[10-K,10-Q,20-F,40-F]`, each with a default test), `tests/adapters/edgar_transport.py` (shared `MockTransport` router over `tests/fixtures/edgar/`: an unmatched URL fails the test; throttle made negligible through settings, a very large `edgar.requests_per_second`) · Tests: `tests/adapters/test_edgar_source.py` (tests set `edgar.index_first_year=2024` and a fixed clock of 2026-09-25T00:00Z, with a second clock of 2026-10-02 for the settle test; the router serves the recorded 2024 Q1 index, a header-only `form.idx` for every quarter in between, and a synthetic one-line index for the clock's quarter naming the recorded KLX 25-NSE, stated as synthetic: `known_at` is the recorded acceptance instant, tz-aware, `provenance = filing`; a filer with no issuer form is dropped and counted; a synthetic exchange-CIK line (1354457) for the same 25-NSE is dropped and the exchange is not an issuer; an accession absent from the submissions lands in `.unstamped_filings` and nowhere else; no-look-ahead test that a filing date never becomes a `known_at`; `since` filters the result but not the set of fetched URLs; a quarter fetched before its settle days is not cached and a settled one is; a 404 for the open quarter yields no rows and no error; a second call fetches only the unsettled quarters and stamps nothing already stamped; the bulk path runs on a small zip built in the test from the recorded submissions, followed by the per-CIK top-up; a truncated cache file is not served; snapshot rows carry `fetched_at` at or before the injected clock and `provenance = snapshot`; missing `sec_edgar_user_agent` raises `EdgarCredentialsError` before any request and the secret appears in no exception text) · Depends on: T11 · Review: quant-auditor, safety-reviewer. The owner runs `filing_index()` once on the PR branch with their keys (command in the PR) and pastes the issuer-CIK count, kept-row count, unstamped count, peak memory and runtime; kept rows include every Form 3, 4, 5, 144 and 13F filed under an issuer CIK. The 400-line budget excludes the synthetic index files and the zip built in the test; if the adapter alone passes it, split the bulk path into a new task depending on T11b and add it to T19's list before opening the PR (T11d to T11f are taken by #216).
- [ ] **T11c: EDGAR FSN data sets, fetch, parse and record.** Files: `src/tradepartner/adapters/edgar_raw.py` (`fsn_periods()`: reads the data-set page `https://www.sec.gov/data-research/sec-markets-data/financial-statement-notes-data-sets` (#174 S1) once per run and returns every href matching `/files/dera/data/financial-statement-notes-data-sets/(\d{4}q[1-4]|\d{4}_\d{2})_notes\.zip`, oldest first, raising if none match, never a hardcoded quarter-to-month boundary, because the SEC rolls months into quarters after the fact (#174 F1, F10); `fsn_zip(period)`: streams the zip into `edgar.cache_dir/fsn/` through a new `_stream_to_with_headers` (temp file, `os.replace`, returns the path and the response headers so `Last-Modified`, `ETag` and `Content-Length` go into the period manifest); the zip is deleted after extraction and a period whose extraction cache exists is not fetched again; any 404 for a listed period fails the chunk; `fsn_validators(period)`: one plain unconditional `HEAD` behind the same throttle, retry and User-Agent, sent only for cached periods still listed on the page, its validators compared with the manifest, a change reported as a re-issue and not re-extracted, and a cached period no longer listed reported as rolled up, not failed; if sec.gov returns no usable validator the run message says re-issues are undetected), `src/tradepartner/adapters/edgar.py` (`normalize_class_member(raw)`, pure: drops any `prefix:` and a trailing `Member`, so `us-gaap:CommonClassAMember` and FSN's `CommonClassA` are one name; `parse_cover_page` calls it too, with `tests/adapters/test_edgar.py` expectations updated; `parse_fsn(sub, num, txt, dim)`, pure, takes row iterables the caller has already filtered while streaming each zip member (DuckDB `read_csv` restricted to the four `dei` names and the cover-page `stmt`), and returns `(records, failures)`: one **unstamped** record per accession with accession, CIK, base form, the listings from `Security12bTitle`, `TradingSymbol` and `SecurityExchangeName` grouped by `dimh`, rows carrying a non-null `coreg` or naming any axis other than `ClassOfStock` excluded exactly as `parse_cover_page` skips other axes, per-class `EntityCommonStockSharesOutstanding` from `num` with `class_member` decoded from `dim.segments` through `normalize_class_member`, and the filing's SIC from `sub.sic`; and one failure per accession whose rows do not parse, never raising for one bad accession. Records carry no timestamp and never read `sub.accepted`, `filed` or `period`: stamping is T11d's, at read time, from the submissions acceptance cache. Class matching downstream stays ingest's letter rule, so a member with no class letter on a multi-class issuer stays unmatched and appears in ingest's existing `unmatched … facts` count, which the owner accepts), `src/tradepartner/adapters/edgar_source.py` (`_ensure_fsn()`: runs once per instance on the first `cover_pages`, `facts` or `filing_headers` call and raises if `filing_index` has not run; extracts every period from `edgar.fsn_first_year` that has no extraction cache, oldest first, keeping every accession whose base form is in `edgar.cover_page_forms` or `edgar.header_forms` (the issuer filter is applied at read time, so a CIK that becomes an issuer later keeps its earlier rows); kept rows are regrouped into per-CIK cache files `edgar.cache_dir/fsn/v{FSN_VERSION}/{cik}.json`, like the stamps cache, so a per-CIK read is one file, and a per-period manifest holds the zip's content hash, its validators, the accessions extracted, the accessions that failed extraction and `committed: false` until T11f's `record_failures()` sets it after an `ok` commit; an accession present in two periods keeps the first extracted and the duplicate is counted on `.fsn_duplicates`; re-issues and roll-ups are counted on `.fsn_reissued`; a period is re-extracted only when `FSN_VERSION` changes, a new constant separate from `PARSER_VERSION`, which stays the stamps' key, so a bump re-extracts only the FSN caches and the PR states that cost, a re-download of every period), `src/tradepartner/ingest.py` and `tests/test_ingest.py` (`_source_counts` also reads `fsn_reissued` and `fsn_duplicates`, plain attributes set in the fetch pass), `src/tradepartner/cli_record.py` (`python -m tradepartner.cli_record fsn`, a target that needs only `SEC_EDGAR_USER_AGENT`, no Alpaca keys, and records the data-set page as `tests/fixtures/edgar/fsn/page.html` plus periods `2025_10` and `2026_02` trimmed to the fixture accessions, scrubbed as in T3), `src/tradepartner/config.py` and `tests/test_config.py` (`edgar.fsn_first_year`, `int`, `ge=2009`, default 2015, one year before the 2016 price start so the latest SIC before any T ≥ 2016 is present) · Tests: `tests/adapters/test_edgar_fsn.py` over the recorded periods under `tests/fixtures/edgar/fsn/` (the dual-class issuer 0001652044, accession 0001652044-26-000018, in `2026_02`; the plain issuer's accession 0000320193-25-000079 in `2025_10`; plus synthetic rows, stated as synthetic, added to the trimmed members in the test: an issuer with a `coreg` row, a fact with a filer-custom member, a row on a non-class axis, a non-issuer CIK, and one accession whose rows do not parse): `fsn_periods` returns the fixture page's periods oldest first and raises on a page with no matches; `parse_fsn` yields the listing triples per class, per-class shares whose `class_member` equals `parse_cover_page`'s for the same recorded filing, and one SIC per accession; `coreg` and other-axis rows are excluded; the unparsable accession is one failure and the others still parse; a fixture accession whose `sub.accepted` (Eastern, after 17:30) and `sub.filed` differ from its submissions acceptance yields records carrying no timestamp at all, so nothing downstream can stamp from FSN (no look-ahead); `_ensure_fsn` before `filing_index` raises; a cached period makes no `GET`; a `HEAD` whose validators changed is counted as a re-issue and the period is not re-extracted; a cached period missing from the page is counted as rolled up; a 404 for a listed period fails; `edgar.fsn_first_year` bounds the periods requested; an accession in two periods is kept once and counted; the router serves one route per fixture period and checks the request method for the validator tests; the recorder target writes the page and the trimmed members and the scrub leaves no email or User-Agent; the PR records peak memory of extraction on the largest quarterly file) · Depends on: T11b; not claimable until the #216 amendment is merged, which records owner decisions (1) and (2); if it passes the size budget, split `fsn_periods`, `fsn_zip` and `fsn_validators` into a new task depending on T11b and add it to T11d's list before opening the PR · Review: quant-auditor, safety-reviewer. **Owner decision (1), recorded by merging #216:** cover pages, per-class shares and SIC come from the SEC Financial Statement and Notes data sets per [#174](../research/2026-09-25-cover-page-facts-at-scale.md), with per-filing downloads only for accessions FSN does not yet hold and ranged SGML headers only for `edgar.header_forms` filings absent from FSN; sub-choices: a per-document cache entry wins over FSN rows for the same accession; `edgar.fsn_first_year` is 2015; the lag-window gap fill runs on every nightly run; a re-issued FSN period is reported, not re-extracted; 8-K stays out of `edgar.cover_page_forms` by owner choice (P1(d) found 8-K covers in FSN) but is in `edgar.header_forms`, so a de-SPAC's new SIC arrives with its 8-K from FSN, or by one ranged header inside the lag window when FSN's SIC is blank. Expected cost from #174, replaced by T11f's measurement: backfill from 2015 about 56 FSN files and 20 GB transient in 0.5 to 2 h with one zip on disk at a time, plus 2,000 to 8,000 lag-window documents with up to a few hours of parsing, 10,000 to 25,000 ranged registration headers (periodic and 8-K headers only inside the lag window, a few thousand), and the Form 25 documents, whose count T11f measures; nightly about 80 documents and 80 ranged headers, and about ten times that on peak filing days.
- [ ] **T11d: EDGAR `FilingSource` adapter, cover pages and headers from FSN.** Files: `src/tradepartner/adapters/edgar_source.py` (`cover_pages(cik)`: the FSN pages of the CIK whose base form is in `edgar.cover_page_forms`, each stamped at read time from `_load_stamps(cik)`, so an accession stamped on a later run appears on that run; an FSN accession with no stamp, or whose stamp record has `accepted_at = None` (cached as unstampable), is excluded and not counted again, because the index already reported it on `.unstamped_filings`; plus a per-filing parse for every accession of the CIK with `isInlineXBRL = 1`, base form in `edgar.cover_page_forms`, absent from every cached FSN period (an accession that failed FSN extraction counts as absent) and accepted on or after the first day of the newest cached period: the root copy of `primaryDocument`, never the `xsl.../` rendering, `parse_cover_page(accepted_at=...)` from the submissions, the whole `CoverPageParse` with `facts` cached by accession under `COVER_VERSION` with its stamp stripped, so T11e reads per-class shares without a re-download, the document deleted after parsing; an older iXBRL cover-form accession absent from FSN is not fetched and is counted on `.fsn_missing`, except one that failed FSN extraction, which `.failed_filings` already counts, because FSN's monthly cutoff is undocumented (#174 UNVERIFIED) and a filing FSN omits must be visible, not silently uncovered. One accession is never taken from both sources: a per-document cache entry for it (`COVER_VERSION` for cover pages, `HEADER_VERSION` for headers), when present, wins over its FSN rows, so a rebuild reproduces the same records; deleting `edgar.cache_dir` or bumping `COVER_VERSION` or `HEADER_VERSION` switches such an accession to its FSN rows, which the store keeps as a revision at that run's `ingested_at`, never as a rewrite. `filing_headers(cik, forms)`: a `FilingHeader` built from `sub.sic` for each FSN accession of the CIK whose base form is in `forms`, stamped the same way, with no request, as the `FilingSource` contract says; a ranged `filing_sgml_header` only for the CIK's filings whose base form is in `forms` and absent from FSN, and among those only registration forms (S-1, F-1, 10-12B) from 1 January of `edgar.header_first_year`, while periodic forms and 8-K get a ranged request only inside the lag window (8-K cover tagging began in 2019 to 2021, so older 8-Ks are absent from FSN and would cost hundreds of thousands of requests); an FSN accession in `forms` inside the lag window whose `sub.sic` is blank (#174: FSN's blanks are 8-Ks) also gets a ranged header, so a de-SPAC's new SIC still arrives with its 8-K; always `parse_sgml_header(text, cik=cik)`, a header whose accession is not the requested one raising `ValueError` until T11f's policy; parsed headers cached by accession under `HEADER_VERSION` with their stamp stripped. Every per-document record, cover page or header, is re-stamped at read time from `_load_stamps(cik)` (`dataclasses.replace`) exactly as FSN records are, never from the header's own ACCEPTANCE-DATETIME or the cached parse, so a `PARSER_VERSION` re-stamp reaches every record and a rebuild reproduces the same records. Registration forms (S-1, F-1, 10-12B) close the SIC gap between an issuer's registration and its first XBRL filing, so rule 5 can fire for a SPAC before its first periodic filing; the `_t11c()` placeholder's message changes to name T11e for `facts` and T11f for `delistings`), `src/tradepartner/ingest.py` and `tests/test_ingest.py` (`_source_counts` also reads `fsn_missing`), `src/tradepartner/config.py` and `tests/test_config.py` (`edgar.header_forms`, default `["S-1", "F-1", "10-12B", "8-K", "10-K", "10-Q", "20-F", "40-F"]`, 8-K included so a de-SPAC's new SIC arrives without waiting for its first 10-Q; registration forms cost a ranged request from `edgar.header_first_year`, periodic forms and 8-K only inside the lag window; `edgar.header_first_year`, `int | None`, default `None` meaning `edgar.fsn_first_year`, so an override of one follows the other; `edgar.cover_page_forms` unchanged), `src/tradepartner/store/classify.py` (`_evidence` calls `source.filing_headers(cik, settings.edgar.header_forms)` instead of `settings.master.issuer_forms`; SIC stays point-in-time because every header, FSN or SGML, is stamped at read time from the submissions acceptance) · Tests: `tests/adapters/test_edgar_source_cik.py` (the router serves the fixture page, the two recorded FSN periods and, for every other listed period, an empty zip whose `sub`, `num`, `txt` and `dim` members are header-only; it adds synthetic index lines for 0000320193-25-000079 and 0001652044-26-000018 so both are stamped, with the test clock and `edgar.fsn_first_year` set so both fall inside FSN's range; submissions served trimmed to the recorded accessions, so nothing unrecorded is requested: cover pages stamped by accession and tz-aware from the submissions cache, never from FSN; an FSN accession with no stamp on the first run is absent, then present after the stamp arrives, with no double count; an unstampable stamp record counts as no stamp; an accession present in FSN and already parsed per document keeps the per-document rows, for cover pages and for headers, and switches to FSN after a `COVER_VERSION` or `HEADER_VERSION` bump; a lag-window filing is fetched per document once and cached; an FSN-failed accession inside the lag window is fetched per document and is not counted as missing, and one older than the lag window is neither fetched nor counted as missing; an older cover-form accession absent from FSN is not fetched and is counted; headers come from FSN with no request for FSN accessions, filtered by `forms`; a ranged request goes only to registration forms absent from FSN on or after `edgar.header_first_year` and to periodic or 8-K filings absent from FSN inside the lag window, never to an older 8-K; a lag-window FSN 8-K with a blank SIC gets one ranged header; an SGML header whose ACCEPTANCE-DATETIME differs from the submissions acceptance is served at the submissions acceptance; a cached cover page and a cached header make no HTTP request; a second call of `cover_pages` and `filing_headers` for a CIK with no new accession makes no request; a header for another accession raises; an HTTP error propagates, nothing is swallowed into an empty result), `tests/store/test_classify.py` (the call site passes `edgar.header_forms`; an override of the key changes the forms requested; rule 5 fires from an FSN SIC stamped before T and not from one stamped after) · Depends on: T11c · Review: quant-auditor, safety-reviewer.
- [ ] **T11e: EDGAR `FilingSource` adapter, facts.** Files: `src/tradepartner/adapters/edgar_source.py` (`facts(cik, names)`: company facts through `parse_company_facts`, from `companyfacts.zip` under the same threshold rule as stamping and per-CIK `company_facts` otherwise, parsed facts cached per CIK keyed by `PARSER_VERSION` and the CIK's latest stamped accession whose base form is in `edgar.cover_page_forms` (so a 10-K/A invalidates), re-fetched only when that changes, **plus** the per-class shares of the CIK's cover pages, FSN rows for FSN accessions and `CoverPageParse.facts` for lag-window accessions, whose name is in `names`, stamped and filtered exactly as T11d's `cover_pages`, de-duplicated on accession, fact name, `as_of_date` and class member, because the company-facts API drops dimensioned facts and dual-class shares exist only on the cover page; a collision with different values raises `ValueError` and fails the chunk until T11f's policy replaces that, and a collided key serves neither value while it is skipped or quarantined) · Tests: `tests/adapters/test_edgar_source_cik.py` (facts stamped by accession and tz-aware from the submissions cache, never from FSN; the dual-class recording yields per-class shares from FSN; the plain-issuer recording, whose accession 0000320193-25-000079 is in both its company facts and its FSN cover page, yields one shares record; an unstamped FSN accession's shares are absent, then present after the stamp arrives; a lag-window accession's shares come from its per-document parse; a forged collision raises and serves neither value; a second `facts` call for a CIK with no new accession makes no request; the bulk and per-CIK company-facts paths both cache by the base-form key) · Depends on: T11d · Review: quant-auditor, safety-reviewer.
- [ ] **T11f: EDGAR `FilingSource` adapter, delistings, failure policy and backfill measurement.** Files: `src/tradepartner/adapters/edgar_source.py` (`delistings(since)`: the index's 25, 25/A, 25-NSE and 25-NSE/A rows over full history, root primary documents through `parse_delisting`, de-duplicated by accession, cached the same way, then `accepted_at >= since`. A 25 or 25-NSE whose `primaryDocument` is not an `.xml` file is not downloaded and is not a filing failure: it is counted on `.pre_xml_delistings` and the measurement records the count, because `parse_delisting` reads XML only and the index runs from 1993. Failure policy, owner decision (2): a filing whose parse raises `ValueError`, whose facts collide with different values, whose SGML header carries another accession, or whose own document or header answers 404 or 410 is skipped, never raised: its accession, the error class, the base form and a hash of the message go to `failed_filings.json` under `edgar.cache_dir` (keyed by its own `FAILURES_VERSION`, read, merged and written through a temp file and `os.replace`) with a count of consecutive counted days on which it failed identically; the run stays `ok`; the adapter exposes `.failed_filings` and `.quarantined`. Counts advance at most once per Eastern calendar day, the day read from the adapter's clock when `record_failures()` runs, so a backfill that commits after midnight counts on the commit day, and only when the EDGAR chunk commits `ok`, so a dry run, a chunk that fails later and three test runs in one evening never advance them, and a second writer on the same day is idempotent. An accession that failed identically on `edgar.max_filing_failures` consecutive counted days (default 3) is quarantined: not retried until its entry is deleted by hand or `FAILURES_VERSION` changes, so one bad filing among a million never blocks the nightly ingest; a different error message resets its count. An FSN accession whose rows failed extraction is recorded in its period manifest, counted on `.failed_filings`, never quarantined and retried only when `FSN_VERSION` changes, because the zip is gone; it counts as absent from FSN, so T11d's lag-window rule applies to it, and it is never counted on `.fsn_missing`, inside the lag window or not, because `.failed_filings` already counts it. `check_failures()`, called by `_prefetch` after the fetch pass and before the lock, raises `FilingFailuresError` so the chunk fails with a failed run row and nothing written when, computed separately for FSN extraction, over every period whose manifest is still uncommitted rather than only those extracted this run (the cache and zip deletion happen before the check, so a period that failed the chunk must fail it again until fixed, never pass as a cache hit), and for per-document fetches (cover pages, headers, delistings), this run's failures are at least `edgar.min_failed_filings` (default 5) **and** above `edgar.max_failed_filing_share` (default 0.01) of the accessions whose extraction was attempted this run in that group, cache hits and quarantined accessions excluded, or when one (error class, base form) pair, over `failed_filings.json` and the FSN manifests together, has at least `edgar.min_failed_filings` distinct accessions not marked `accepted: true`; the owner sets `accepted` by hand after review, accepted entries stay quarantined and leave the rule, so a handful of genuinely malformed filings of one form cannot fail every run until a code change, so a nightly run of about 80 documents is not failed by one bad filing, a new FSN month's thousands of accessions cannot dilute a per-document bug, and a parser bug that breaks a few 20-Fs a night cannot hide behind quarantine; any other HTTP error, a corrupt zip and rate-limit exhaustion still fail the chunk), `src/tradepartner/ingest.py` and `src/tradepartner/backfill.py` (`_run_source` gains `after_commit: Callable[[], None] | None`, called after the `try` block, outside it, only for a committed `ok` run that is not a dry run, an exception from it appended to the returned message only, the committed run row never rewritten; `ingest_session` passes it through a per-source dict like `prepare`, edgar only, and `backfill` passes it for its EDGAR chunk, both as `getattr(<the adapter under _Recorded>, "record_failures", None)`, so a fixture source and the alpaca chunk never call it; `_prefetch` unwraps `_Recorded` as `_source_counts` does and calls `check_failures()` if present; `_source_counts` also reads `pre_xml_delistings`, `failed_filings` and `quarantined`; #173 edits the same file, so whichever lands second merges main first), `src/tradepartner/config.py` and `tests/test_config.py` (`edgar.max_filing_failures`, `edgar.min_failed_filings`, `edgar.max_failed_filing_share`) · Tests: `tests/adapters/test_edgar_source_cik.py` (the recorded KLX 25-NSE, reached through the synthetic index line, yields its class title and exchange; `delistings(since)` filters on `accepted_at`; a cached delisting makes no request; a pre-XML Form 25 is not downloaded and is counted), `tests/adapters/test_edgar_failures.py` (a forged parser `ValueError`, a forged collision, a forged header mismatch and a 404 for one document each skip that accession, leave the run `ok`, appear on `.failed_filings` and in the run message, and are retried on the next counted day; the same failure on `edgar.max_filing_failures` consecutive counted days quarantines the accession and the next run makes no request for it; two runs on one Eastern day advance the count once; a commit after Eastern midnight counts on the commit day; a dry run and a failed chunk advance nothing; a different error message resets the count; a `FAILURES_VERSION` bump retries it; an FSN extraction failure is counted, never quarantined and served through the lag window; a period whose extraction failed the chunk fails it again on the next run with no download, until the parser is fixed or the pair is accepted; failures at or above `edgar.min_failed_filings` and above `edgar.max_failed_filing_share` of attempted per-document accessions fail the chunk before the lock with no row written, the same count below `edgar.min_failed_filings` does not, thousands of clean FSN accessions in the same run do not dilute it, and five distinct accessions with one (error class, base form) pair across days, FSN manifest failures included, fail the chunk even when each day's share was small, and no longer do once marked `accepted`; a 500 still fails the chunk), `tests/test_ingest.py` and `tests/test_backfill.py` (`failed filings: K; quarantined: Q; pre-XML delistings: P` in the EDGAR run message when the source exposes them; `after_commit` called only after a committed `ok` EDGAR chunk, never for alpaca, a fixture source or a dry run, and its exception leaves the status `ok` and the committed row untouched; `check_failures` raising fails the chunk with one failed run row) · Depends on: T11e; if it passes the size budget, split the failure policy into a new task depending on T11e and add it to T19's list before opening the PR · Review: quant-auditor, safety-reviewer. **Measurement:** T19's CLI does not exist yet, so the owner runs a `spike/` script over every issuer CIK on the PR branch and the PR records the request count, `edgar.cache_dir` size, peak memory, the Form 25 document and pre-XML counts and the runtime of the real backfill, and the same for one run on a later day; if the backfill exceeds one overnight run, the owner decides before T19 whether it resumes across nights through the adapter's per-accession caches (EDGAR is one chunk in `backfill.py`, there is no chunk to resume) or is capped per run by a new `edgar.max_cover_downloads_per_run`.
- [x] **T12: Alpaca parsers.** Files: `src/tradepartner/adapters/alpaca_prices.py` · Tests: `tests/adapters/test_alpaca_prices.py` (raw closes; `known_at` per timing rule; actions proxy; resolution through master) · Depends on: T3, T7, T8b · Review: quant-auditor, safety-reviewer. Runs in parallel with T11.
- [x] **T13: Universe rules.** Files: `src/tradepartner/universe.py` (`universe_as_of(..., settings: Settings | None = None)`, defaulting to the loaded config as `adjusted_prices_as_of` does, used for every `universe.*` value and passed to every callee that takes one: `listings_as_of`, classification, and `adjusted_prices_as_of` if it calls it; #102), `tests/lookahead/test_universe_invariance.py` · Tests: `tests/test_universe.py` (rules 1–8 in order; dual-class; split-adjusted cap with ex-date ≤ T only; future-ex-date split ignored; stale shares; liquidity flag off recorded; output records settings; an override `Settings` changes the result without touching the environment) · Depends on: T9, T10 · Review: quant-auditor.
- [x] **T14: Universe config tests and literal check.** Files: `tests/test_universe_config.py`, `tests/test_no_literals.py` (AST check on `universe.py` and `gap.py`) · Tests: one override test per `universe.*` key · Depends on: T13 · Review: quant-auditor.
- [x] **T15: Survivorship gap.** Files: `src/tradepartner/gap.py` (`survivorship_gap(..., settings: Settings | None = None)`, defaulting to the loaded config, used for every `gap.*` value and passed through to `universe_as_of`; #102), `tests/lookahead/test_gap_invariance.py` · Tests: `tests/test_gap.py` (formula; clean merger not missing; three side categories; an override `Settings` changes the result without touching the environment) · Depends on: T13 · Review: quant-auditor. Runs in parallel with T14.
- [x] **T16: Single-session ingest.** Files: `src/tradepartner/ingest.py` · Tests: `tests/test_ingest.py` (idempotent re-run; staleness on holiday, weekend, pre-close, half day; stale → non-zero and unchanged; per-source atomic chunk; lock retry with a closing reader and with a persistent writer) · Depends on: T11, T12 · Review: quant-auditor, safety-reviewer. Runs in parallel with T13–T15.
- [x] **T17: Backfill and resume.** Files: `src/tradepartner/backfill.py` · Tests: `tests/test_backfill.py` (monthly chunks per source; mid-chunk failure leaves earlier chunks; resume from `chunk_cursor`; lock released between chunks) · Depends on: T16 · Review: quant-auditor.
- [x] **T18: Health metrics.** Files: `src/tradepartner/health.py` · Tests: `tests/test_health.py` (each metric on the fixture store incl. delisted names and static reliance; each integrity rule fails on an injected violation incl. bar-after-delisting; guarded SIC) · Depends on: T15, T17 · Review: quant-auditor.
- [ ] **T19: CLI.** Files: `src/tradepartner/cli.py`, `pyproject.toml` (`[project.scripts]` only) · Tests: `tests/test_cli.py` (`ingest`, `health --check` exit codes, and it warns when the last EDGAR run row reports any quarantined accession (T11f), `dashboard` launches, `export` writes Parquet per table; `ingest` builds `EdgarFilingSource` from settings, tested with an injected `MockTransport`; a missing `SEC_EDGAR_USER_AGENT` exits non-zero with no secret in the output) · Depends on: T18, T21a, T11f; and #172 merged (not enforced by `team.py`) · Review: safety-reviewer (no secrets in output).
- [x] **T20: `Broker` interface and fake broker.** Files: `src/tradepartner/adapters/{broker,fake_broker}.py` · Tests: `tests/adapters/test_fake_broker.py` · Depends on: T4 · Review: safety-reviewer. Runs in parallel with T5–T19.
- [x] **T21a: Dashboard shell.** Files: `src/tradepartner/dashboard/{__init__,app}.py` (Streamlit app entry; short-lived read-only connection via `store.db`; "store busy" state when the store is locked; "no store yet" state when the file does not exist; navigation with an empty health-page placeholder in `app.py`, which T21 points at `health_page.py`) · Tests: `tests/dashboard/test_app.py` (headless render; read-only connection; busy state when locked; no-store state) · Depends on: T4 · Review: `/code-review`. Runs in parallel with T5–T20, so UX work starts early (#53).
- [x] **T21: Data-health page.** Files: `src/tradepartner/dashboard/health_page.py`, `src/tradepartner/dashboard/app.py` (navigation entry only), `src/tradepartner/dashboard/theme.py` and `.streamlit/config.toml` (tokens and Plotly template per [design/dashboard.md](../design/dashboard.md), #165) · Tests: `tests/dashboard/test_health_page.py` (headless render shows every metric; reads only through the shell's read-only connection) · Depends on: T18, T21a · Review: `/code-review`.
- [ ] **T22: Scheduling runbook and unattended evidence.** Files: `docs/runbooks/scheduling.md` (launchd plist; PATH for `uv`; working dir and `.env`; sleep vs power-off; logs; TCC) · Tests: none; evidence = five consecutive scheduled `ok` runs, collected by the owner · Depends on: T19; and #173 merged (not enforced by `team.py`) · Review: safety-reviewer. Runs in parallel with T21.
- [ ] **T23: `data-validator` agent and close-out.** Files: `.claude/agents/data-validator.md`, `docs/ways-of-working/agents.md`, `docs/STATUS.md`, `CHANGELOG.md`, this plan · Tests: agent runs `health --check` and the suite on the owner's real store; PR includes that report, the T22 evidence, and the real-store 2020 universe check (non-empty, includes a later-delisted name) · Depends on: T21, T22 · Review: quant-auditor on the report.

**Parallel lanes** (separate worktrees, non-overlapping files): T1 → {T2, T4}; T2 → T3 (owner); T4 → {T5, T20, T21a}; T5 → T6 → {T7, T8}; T8 → T8b → T9; {T7, T8b} → T10; {T3, T9} → T11 → T11b → T11c → T11d → T11e → T11f; {T3, T7, T8b} → T12; {T9, T10} → T13 → {T14, T15}; {T11, T12} → T16 → T17; {T15, T17} → T18; {T18, T21a} → T21; {T18, T21a, T11f} → T19; T19 → T22; {T21, T22} → T23.

## Chains (for team claims)

Dependent tasks one team should keep, in order. A chain is a preference, not a lock: every task is still claimed one at a time with `scripts/team.py claim` ([teams.md](../ways-of-working/teams.md)). Chains that wait on another chain's head cannot start until it merges, so the ready frontier is narrow early in the phase; merge chain heads first.

| Chain | Tasks | Starts when |
|---|---|---|
| universe | T5 → T6 → T7 → T10 | T4 merged (T10 also waits for T8b from the master chain) |
| broker | T20 | T4 merged |
| master | T8 → T8b → T9 | T6 merged |
| parsers | T11 → T11b → T11c → T11d → T11e → T11f; T12 in parallel | T3 (owner) plus T9 for T11; T7 and T8b for T12 (T11c also waits for the #216 amendment) |
| rules | T13 → T14, T15 | T9 and T10 merged |
| ux | T21a → T21 | T4 merged (T21 also waits for T18 from the pipeline chain) |
| pipeline | T16 → T17 → T18 → T19 | T11 and T12 merged (T18 also needs T15; T19 also needs T21a, T11f and #172) |
| close-out | T22 → T23 | T19 and #173 merged (T23 also waits for T21) |
| fixes | open `size:S` issues with no `team:` label | any time |

## Verification

End to end, on a clean checkout:
1. `uv run pytest` green with no `.env` and no network, including truncation-invariance and broken-adapter tests.
2. With the owner's `.env`: `uv run tradepartner ingest --source all --backfill` completes with no `--since`, in one run or resumed per the owner's T11f decision (runtime, EDGAR request count and `edgar.cache_dir` size recorded, and the same for one follow-up daily run); a later `ingest --source all` after the settle delay shows `rows_added = 0` for Alpaca, with EDGAR deltas explained in the run row.
3. `uv run tradepartner health --check` exits 0; numbers match a direct DuckDB query pasted alongside.
4. `uv run tradepartner dashboard` shows the same (screenshot).
5. Real-store month-end universe inside the backfill window, non-empty with a later-delisted name (query pasted).
6. Five consecutive scheduled `ok` runs; `data-validator` report attached (T23).

## Rollback

The store is one file: delete it and re-run `ingest --backfill`. The launchd plist is installed by hand and unloaded with one command (runbook). Dependencies are removed by reverting T1. No migrations touch anything outside the store file.
