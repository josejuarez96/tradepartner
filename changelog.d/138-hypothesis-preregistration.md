### Added
- Phase 3 T35: `tradepartner.backtest.hypothesis` (`parse_file`, `frozen_keys`, `required_keys`, `frozen_params`, `register`, `load_frozen`), the hypothesis template `docs/templates/hypothesis.md` and the `hypotheses/` row in `docs/README.md` (#138).
