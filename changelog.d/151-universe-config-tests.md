### Added
- `tests/test_universe_config.py` (one override case per `universe.*` key, both override routes) and `tests/test_no_literals.py` (AST check on `universe.py` and `gap.py`, moved out of `tests/test_universe.py`) (T14, #151).
