### Added
- Phase 3 T37b: `tradepartner.backtest.engine` (`run`, `BacktestResult` with equity, rebalance and weight rows, position values, marking frames, targets and `stitched_returns()`) and `tradepartner.backtest.fills` (`apply_trades`, `FillResult`, `Trade`); `DataProvider.raw_prices` (#155).
