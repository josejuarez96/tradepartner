### Added
- `tradepartner.ingest`: `ingest_session`, `expected_session`, `fact_rows` (XBRL `EntityCommonStockSharesOutstanding` → `shares_outstanding`, class member → security), `IngestResult`/`SourceRun` with `ok`, `stale`, `failed`, `locked` statuses (T16, #157).
